
## 2.6 重复货架环境下的多候选时序消歧与失败关闭

### 2.6.1 本部分解决的问题

在大型超市中，不同货架通道可能具有相近的宽度、朝向和结构排列。仅根据某一时刻的局部结构进行匹配时，真实通道和相邻的相似通道可能得到接近的匹配结果。

如果系统在每次匹配时都直接选择当前代价最小的候选位姿，就可能出现以下问题：

- 前一时刻定位在真实通道；
- 下一时刻因为局部遮挡、动态顾客或结构重复，另一通道的瞬时匹配代价略低；
- 系统立即切换到另一通道；
- 设备在门店地图中的位置发生不符合实际运动的横向跳变。

因此，本部分不依赖单帧最优结果直接校正地图位姿，而是同时保留多个候选，通过连续匹配更新建立若干“定位假设”，再结合跨帧支持程度、候选唯一性、匹配质量和运动连续性确定是否允许校正。

本部分承接2.5的输出：

```
2.5：在门店先验地图中产生若干候选位姿及其匹配质量
  ↓
2.6：对这些候选进行跨帧关联、时序消歧、可信判定和安全校正
```

### 2.6.2 “帧”和候选位姿的含义

本部分所称的“帧”，是指移动设备采集的一次同步观测，例如同一采样时刻的彩色图像、深度数据、相机内参、ARKit/VIO位姿和跟踪状态。

它不是专指SLAM数据库中的一个节点。SLAM系统可以从连续采集帧中选择部分帧形成SLAM节点，因此：

- AR采集帧是传感器观测；
- SLAM节点是由部分采集帧生成的图优化节点；
- 二者不能直接等同。

更准确地说，假设中的计数只在系统实际执行了一次结构匹配时更新。因此，原文中的“连续支持帧数”和“未命中帧数”，建议分别改为：

- 连续获得支持的有效匹配更新次数；
- 连续未关联的有效匹配更新次数。

这样可以避免让人误以为每一个ARFrame或者每一个SLAM节点都会使计数增加。

2.5输出的每个候选位姿，表示移动设备在门店先验地图坐标系 \(M\) 中的一种可能位置和朝向，即：

$T^{cand}_{M\leftarrow B}$

其中包括平面位置 \(x、y\)、偏航角 \(yaw\) 以及相应的匹配代价或质量分数。

系统再利用同一时刻连续里程计给出的设备位姿：

$T_{O\leftarrow B}$

将候选设备位姿转换为候选地图—里程计对齐关系：

$T^{cand}_{M\leftarrow O} = T^{cand}_{M\leftarrow B} \left(T_{O\leftarrow B}\right)^{-1}$

因此，2.6实际跟踪的是多个可能的“地图坐标系与连续里程计坐标系之间的对齐关系”，而不是跟踪某一片货架点云。

### 2.6.3 匹配代价、瞬时最小代价和质量分数

对于一个候选位姿，系统将当前局部结构点按照该候选位姿变换到门店先验地图中，并查询这些结构点与地图结构之间的距离。多个结构点的距离经过截断和汇总后形成候选匹配代价：

$J(T)=\frac{1}{N}\sum_{k=1}^{N} \min\left(D(Tp_k),d_r\right)^2$

其中：

- $p_k$为当前局部结构点；
- $T$为待评价的候选位姿；
- $D(\cdot)$为先验地图距离场查询；
- $d_r$为鲁棒截断距离；
- $J(T)$越小，表示当前观测与门店地图结构越一致。

几个容易混淆的概念应区分如下：

- “候选匹配代价”：某一个候选位姿对应的 $J(T)$；
- “瞬时最小代价”：本次搜索产生的全部候选中最小的 $J(T)$；
- “当前匹配代价”：当前被跟踪或评价的某一候选在本次更新中的代价；
- “平均匹配质量”：同一定位假设在连续多次有效更新中累积形成的质量；
- “质量分数”：匹配代价经过单调转换后的表达，通常代价越小，分数越高。

本发明不限于某一种具体的代价—分数换算公式。其核心是：既评价候选当前是否匹配，也评价该候选是否在连续观测中稳定成立。

### 2.6.4 定位假设是什么

“定位假设”是系统对地图—里程计对齐关系的一种跨帧解释。

例如，在两个外观和结构都很相似的平行货架通道中，单次搜索可能得到候选A和候选B。系统不会立即断定其中一个必然正确，而是建立两个定位假设：

```
定位假设A：设备位于货架通道A
定位假设B：设备位于货架通道B
```

每个定位假设至少维护：

- 所代表的候选地图—里程计对齐变换；
- 最近一次与该假设关联的候选位姿；
- 当前或累计匹配质量；
- 获得有效支持的次数；
- 连续未关联的有效匹配更新次数；
- 假设当前是否仍处于有效跟踪状态。

定位假设不是一帧数据，也不是一个货架点云，而是一条由连续匹配结果共同支持的候选定位轨迹。

原文中的“候选地图（点云地图）对里程计变换”容易误解，建议改成：

> 候选门店地图—连续里程计对齐变换。

此外，当前假设本身并不需要保存墙钟时间或每次命中的绝对时间戳。假设的新鲜程度可以通过“连续未关联更新次数”等状态表示。进入Recovery恢复过程后，恢复事件的开始、截止和结束时间可以单独记录，不应与定位假设字段混写。

### 2.6.5 新候选如何关联到已有定位假设

“新候选”是指2.5在当前一次匹配更新中产生、但尚未与历史定位假设关联的候选结果。

系统将新候选所代表的 $T^{cand}_{M\leftarrow O}$，与已有定位假设保存的对齐变换进行比较。比较内容主要包括：

- 平面位置差异；
- 偏航角差异；
- 必要时还可结合运动方向和位姿变化连续性。

如果新候选与某个已有假设在地图—里程计对齐空间中足够接近，则将其视为对该已有假设的一次新支持，并：

- 更新该假设的候选对齐关系；
- 累计或刷新支持信息；
- 更新综合匹配质量；
- 将连续未关联次数清零。

如果某个已有假设在本次有效匹配更新中没有关联到候选，则增加其连续未关联更新次数。当连续多次不能获得支持时，该假设可以转为非活跃状态或者被淘汰。

如果新候选与全部已有假设都不接近，则根据该候选建立一个新的定位假设。但新建立的假设只有初始证据，不能立即用于修正地图—里程计对齐关系。

因此，原文中的“同一候选”建议修改为：

> 属于同一定位假设的连续候选。

“同一定位假设”是根据候选所代表的地图对齐关系是否在空间和朝向上连续来判断的，并不是通过“是否来源于同一货架点云”判断的。即使两个货架形状相同，只要它们在地图中的位置不同，也应当形成不同的定位假设。

### 2.6.6 主定位假设如何确定

系统从当前仍然获得有效支持的定位假设中确定优先候选，而不是简单选择某一帧代价最小的结果。

主定位假设的判定可以综合考虑：

1. 是否在本次有效匹配更新中获得候选支持；
2. 最近连续有效更新中的支持程度；
3. 跨帧累计匹配质量；
4. 历史支持的稳定程度；
5. 与其他竞争假设之间是否存在足够的质量差异。

系统可以先按照连续支持程度和综合匹配质量进行排序，再比较排名第一和第二的定位假设。

但“排名第一”不等于“可以直接采用”。排名第一的假设还必须通过可信判定。如果其证据不足，系统不能绕过判定条件而任意选择排名第二的假设。

这种方式能够防止系统在两个相似货架之间仅因为一次微小的代价变化就发生来回切换。

### 2.6.7 可信判定使用哪些数据

定位假设是否可信，至少由以下几类可测量数据共同确定。

1. 结构证据是否充分

检查有效结构点数量，以及结构点在设备周围或视野中的方向覆盖程度。如果结构点过少或者只集中在单一方向，候选位姿容易沿着货架方向滑动，不足以支持可靠校正。

2. 匹配质量是否合格

检查候选匹配代价是否表明当前局部结构与门店地图结构具有足够的一致性。匹配代价过大，说明该候选不能合理解释当前观测。

3. 候选是否具有唯一性

比较最佳候选和次佳独立候选之间的代价或质量差异。如果两个候选结果过于接近，说明仅凭当前结构不能区分两个相似货架通道。

除单次候选差异外，还可以比较两个跨帧定位假设的累计质量差异，判断某个假设是否在时间上持续优于竞争假设。

4. 设备跟踪状态是否可靠

检查产生当前结构观测的同一采集帧中的ARKit/VIO跟踪状态。若跟踪丢失、受限或者姿态质量不足，则不应基于该帧结果修改地图对齐关系。

5. 候选校正幅度是否符合运动连续性

比较候选对齐变换与当前已采用对齐变换之间的平移和偏航角差异。如果候选要求设备瞬间跨越到另一条货架通道，或者产生明显不符合连续运动的旋转，则拒绝自动校正。

专利正文应保护上述多条件联合判定方法，而不必将具体点数、角度、距离和次数写成唯一限定值。具体阈值可作为优选实施例说明。

### 2.6.8 什么是“有界增益渐进更新”

本部分的“全局坐标”是指门店先验地图坐标系 \(M\) 下的业务位置和朝向，不是经纬度坐标，也不是ARKit内部的临时世界坐标。

可信定位假设通过判定后，系统也不必一次性把当前地图—里程计对齐关系直接替换为候选值，而是进行渐进更新：

$T^{new}_{M\leftarrow O} = \operatorname{Interp} \left( T^{old}_{M\leftarrow O}, T^{cand}_{M\leftarrow O}, g \right)$

其中：

- $T^{old}_{M\leftarrow O}$为当前使用的地图—里程计对齐关系；
- $T^{cand}_{M\leftarrow O}$ 为可信候选给出的对齐关系；
- $g$为更新增益；
- $\operatorname{Interp}$表示对平移量和偏航角进行渐进插值。

“有界”包含三层含义：

- 候选误差必须处于允许自动校正的范围内；
- 每次只吸收候选校正量的一部分；
- 每次实际执行的平移和偏航角修正不得超过单次安全上限。

更新后的设备地图位姿为：

$T^{new}_{M\leftarrow B} = T^{new}_{M\leftarrow O} T_{O\leftarrow B}$

这种处理只更新门店地图与连续里程计之间的对齐关系，不重置ARKit/VIO原始里程计，也不直接修改SLAM内部的原始轨迹。

### 2.6.9 失败关闭

如果出现下列任意情况：

- 有效结构点不足；
- 结构视角覆盖不足；
- 候选匹配代价过大；
- 最佳和次佳候选难以区分；
- 跨帧定位假设仍存在竞争；
- ARKit/VIO跟踪状态不可靠；
- 候选要求的校正幅度超过安全范围；

系统均不自动采用“看起来最接近”的货架通道。

此时系统：

1. 保留原有地图—里程计对齐关系；
2. 继续保留连续里程计轨迹；
3. 保存候选、代价、判定结果及拒绝原因；
4. 将定位可信状态标记为弱可信；
5. 若弱可信状态持续存在，则进入后续Recovery扩大搜索流程；
6. 在重新获得可信定位前，不输出缺乏证据支持的绝对门店坐标。

这里的“失败关闭”不是终止扫描或删除观测数据，而是关闭不可信的自动校正和坐标发布能力。

### 2.6.10 图5对应的完整数据处理流程

图5建议按照以下顺序绘制：

```
步骤S261：接收2.5输出的多个候选设备地图位姿及匹配代价
        ↓
步骤S262：利用同一时刻连续里程计位姿，
          将候选设备位姿转换为候选地图—里程计对齐变换
        ↓
步骤S263：按照平移和偏航角邻近关系，
          将当前候选关联到已有定位假设
        ↓
步骤S264：更新各假设的支持程度、综合匹配质量
          和连续未关联次数，并建立必要的新假设
        ↓
步骤S265：从当前获得支持的假设中确定最佳和次佳假设
        ↓
步骤S266：检查结构点数量、方向覆盖、匹配质量、
          候选唯一性、跟踪状态和校正幅度
        ↓
        ┌────────可信────────┐
        ↓                    │
步骤S267：采用有界增益渐进更新地图—里程计对齐关系
        ↓
输出更新后的门店地图位姿
                             │
        ┌──────不可信────────┘
        ↓
步骤S268：保持原对齐关系，记录拒绝证据并标记弱可信
        ↓
步骤S269：若不可信状态持续存在，进入Recovery扩大搜索
```

### 2.6.11 平行货架场景示例

假设移动设备实际位于货架通道A，但由于通道B结构相似，本次匹配同时产生A、B两个候选。

- 候选A与上一时刻主定位假设连续，且连续多次获得支持；
- 候选B虽然某一时刻的瞬时匹配代价略低，但缺乏连续支持；
- 系统保留两个假设，不因候选B单次更优就立即跳转；
- 后续移动过程中，候选A持续与设备运动一致，候选B逐渐失去支持；
- 系统确认候选A具有稳定性和唯一性后，才对地图—里程计对齐关系进行有界渐进校正。

如果A、B两个候选始终难以区分，则系统保持原校正并进入弱可信或Recovery状态，而不是任意选定一个货架通道。
可以。2.7的核心不是另外引入一套定位技术，而是规定：2.5和2.6得到的定位结果在什么情况下可以采用，定位不可靠时怎样重新定位，以及当前定位状态是否允许输出价签地图坐标。

## 2.7 定位状态转换与定位恢复

“状态机”和“Recovery”都属于工程术语。为了便于代理人理解，正文可以使用“定位状态转换规则”和“定位恢复”。如果需要保留英文，只在第一次出现时写成：

> 定位恢复（Recovery）是指移动终端在当前地图定位不可信时，通过扩大搜索范围、连续验证候选结果，重新确定自身在门店先验地图中位置的处理过程。

后文统一使用“定位恢复”，不要反复混用Recovery、Recovery Episode、恢复状态、恢复阶段等不同说法。

## 一、执行主体以及与价签自动定位的关系

本部分的执行主体是移动终端，例如安装MarketScanner的手机。更具体地说，是运行在移动终端中的“先验地图定位模块”。

正文首次出现时建议写成：

> 移动终端中的先验地图定位模块，以下简称“定位模块”。

后文统一使用“定位模块”，不再使用含义不清的“定位器”“系统定位器”或者“业务定位器”。

定位模块使用以下数据判断当前定位是否可靠：

- 连续里程计输出的设备位姿；
- 当前采集的局部结构点；
- 门店先验地图；
- 2.5产生的候选对齐关系；
- 2.6形成的定位假设；
- 移动终端当前的传感器跟踪状态。

本部分与价签自动定位有直接关系，但它不是独立于价签自动定位的第三个目的。

价签自动定位必须知道：识别到价签时，移动终端在门店先验地图中的位置是否可信。如果定位模块处于稳定状态，并且价签识别、深度测量和节点绑定等条件也合格，才能输出价签地图坐标。

如果定位模块处于弱可信、定位丢失或者恢复中：

- 移动终端仍然可以继续识别和保存价签条码内容；
- 仍然可以保存价签图像、深度及其对应的扫描节点；
- 但不能把当时计算出的地图坐标直接作为可信价签坐标发布；
- 后续只有在定位恢复或扫描后处理能够重新证明该坐标时，才能形成正式结果。

因此，2.7的作用是为价签自动定位增加一道定位可靠性判断，防止价签被错误地定位到相邻的相似货架上。

## 二、“定位状态”是谁的状态

本部分的定位状态，属于移动终端中定位模块的状态。

它表示的是：

> 当前门店先验地图与连续里程计之间的对齐关系，是否足以支持自动校正和价签地图坐标输出。

它不是以下对象的状态：

- 不是移动终端硬件自身的运行状态；
- 不是ARKit自身的跟踪状态；
- 不是某个SLAM节点的状态；
- 不是货架的状态；
- 也不是价签本身的状态。

ARKit等传感器的跟踪状态只是定位模块进行判断的一项输入。定位模块综合候选匹配、连续支持、传感器跟踪以及校正幅度等证据，得到最终的定位状态。

## 三、定位状态的统一中文含义

为避免前后用词不一致，建议全文统一使用以下名称。

|英文名称|统一中文名称|通俗含义|
|---|---|---|
|uninitialized|未初始化|尚未建立门店先验地图与连续里程计之间的对齐关系|
|initializing|初始化中|已有初步位置，但还没有得到连续观测的充分支持|
|usable|可用|已经得到可使用的地图定位结果，但稳定证据尚未完全积累|
|stable|稳定|连续多次观测均支持同一定位结果，可以进入正常工作|
|weak|弱可信|仍有大致位置，但当前证据不足以继续安全校正或输出价签地图坐标|
|lost|定位丢失|已经无法证明移动终端当前位于门店地图中的哪个位置|
|recovering|恢复中|定位模块正在扩大搜索范围，重新寻找可信位置|
|manualCorrection|人工校正中|由工作人员选择已知地图位置或方向，帮助重新建立对齐关系|

其中，“人工校正中”是可选实施方式。它不是本发明自动定位恢复方法的必要步骤。如果保留，建议放在2.7末尾单独说明，不要与主要自动流程混写。

还应当区分以下两个概念：

- “恢复中”是定位模块当前所处的定位状态；
- “一次定位恢复回合”是定位模块在恢复中执行的一次有开始条件、有结束条件的处理过程。

## 四、定位状态如何转换

定位状态的基本转换过程如下。

### 1. 从未初始化进入初始化中

移动终端加载门店先验地图，并获得初始位置、人工选择位置或者第一次候选匹配结果后，定位模块开始建立门店先验地图与连续里程计之间的对齐关系。

此时只有初步结果，尚未获得连续观测支持，因此进入“初始化中”。

### 2. 从初始化中进入可用

定位模块连续采集局部结构点，并按照2.5和2.6的方法进行地图匹配和定位假设判断。

当某一定位假设通过基本的结构、匹配质量和安全性判断后，定位模块得到一个可以使用的地图对齐结果，状态进入“可用”。

“可用”表示已有可信依据，但不等于已经允许自动发布价签地图坐标。

### 3. 从可用进入稳定

移动终端继续正常扫描。如果连续多次局部正常匹配均支持同一个定位假设，而且没有出现明显冲突，定位状态由“可用”进入“稳定”。

只有进入“稳定”后，才允许满足自身质量条件的价签观测进入自动定位结果。即使定位状态稳定，价签本身仍需通过识别质量、深度质量和节点绑定等条件。

### 4. 从稳定或可用进入弱可信

出现以下情况时，定位模块可以进入“弱可信”：

- 连续若干次实际匹配未找到可信的定位假设；
- 最佳候选与次佳候选过于接近，无法区分相似货架；
- 当前结构点数量或方向覆盖不足；
- 候选要求的校正幅度明显不符合连续运动；
- 移动终端的传感器跟踪质量下降；
- 当前观测与门店先验地图持续不一致。

弱可信表示系统仍保留之前的位置和原始轨迹，但暂时停止自动采用新的地图校正结果，并停止发布新的价签地图坐标。

### 5. 从弱可信进入定位丢失或恢复中

如果后续正常匹配重新获得充分证据，定位模块可以重新回到可用状态。

如果弱可信持续存在，或者已经无法维持可信地图位置，则：

- 进入“定位丢失”；或者
- 启动一次定位恢复回合，并进入“恢复中”。

### 6. 定位恢复成功后的状态

定位恢复成功后，定位模块先回到“可用”，而不是直接回到“稳定”。

这是因为扩大范围搜索虽然重新找到了可能正确的位置，但还需要由后续局部正常匹配再次验证。只有连续获得新的局部正常匹配证据后，才能重新进入“稳定”。

这种设置可以防止扩大搜索刚找到一个候选位置，就立即把它当作完全可靠的最终位置。

## 五、什么情况下启动定位恢复

不能把“弱可信或者定位丢失时可以恢复”写成没有条件的自动操作。建议明确为满足以下一种或者多种情况时，定位模块才启动定位恢复：

1. 连续多次实际执行的匹配均没有得到可信定位假设；
2. 定位状态已经由稳定或可用下降为弱可信，并且在一定时间或一定次数内未自行恢复；
3. 定位状态已经变为定位丢失；
4. 移动终端的传感器跟踪已经恢复，但门店先验地图与连续里程计之间的对齐关系仍然不可信；
5. 连续出现一个与当前位置相距较远、但跨帧重复出现的候选结果，普通局部搜索无法安全判断是否应当采用；
6. 工作人员主动请求重新定位。

这些触发条件的核心是：

> 普通局部匹配已经不能可靠确定位置，继续沿用旧结果或者直接采用某个远处候选都存在风险，因此需要启动范围更大的重新搜索。

进入定位恢复不等于采用某个远处候选。它只表示定位模块开始扩大搜索范围并重新积累证据。

具体连续次数、持续时间和触发间隔可以作为实施例参数，不宜写成本发明不可改变的必要条件。

## 六、一次定位恢复回合怎样执行

一次定位恢复回合可以按照以下步骤展开。

### 步骤1：建立定位恢复回合

输入包括：

- 当前定位状态；
- 连续未得到可信定位结果的次数；
- 当前传感器跟踪状态；
- 上一次定位恢复的结束情况；
- 是否收到人工重新定位请求。

定位模块判断是否满足恢复触发条件。满足条件时，建立一次新的定位恢复回合，并记录：

- 本次恢复的身份编号；
- 触发原因；
- 开始时间；
- 截止时间；
- 允许执行的有效搜索次数。

输出是一个已经建立、等待搜索的定位恢复回合。

这里的“截止时间”是指本次定位恢复最晚应当结束的时间。优选采用只向前累计、不受手机系统时间调整影响的计时方式。

“冷却期”是指一次自动恢复结束后，暂时不立即再次启动自动恢复，防止定位模块反复启动和退出恢复流程。

### 步骤2：检查本次观测是否可以用于恢复搜索

定位模块读取：

- 当前局部结构点；
- 当前连续里程计位姿；
- 门店先验地图；
- 当前传感器跟踪状态。

定位模块首先检查数据是否有效。例如：

- 是否有有效深度；
- 传感器跟踪是否可以使用；
- 结构点是否足够；
- 当前是否具备执行搜索所需的计算资源。

只有实际完成一次候选搜索，才计为一次有效搜索。

如果本轮没有有效深度、传感器跟踪不可用、结构点不足、计算任务繁忙或者本轮受到计算频率限制，则本轮只是等待，不应计为一次失败的恢复搜索，也不应消耗有效搜索次数。

### 步骤3：扩大搜索范围并产生多个候选

普通定位主要在当前预测位置附近进行局部搜索。进入恢复状态后，定位模块在更大的平移和偏航角范围内搜索。

处理过程仍然沿用2.5的方法：

1. 使用当前局部结构点查询门店先验地图距离场；
2. 从较粗的搜索范围开始查找可能位置；
3. 对较好的区域进一步细化；
4. 保留多个彼此分离的候选对齐关系；
5. 为每个候选计算匹配代价和质量信息。

输出是多个“候选对齐关系”，而不是一个可以直接采用的最终位置。

### 步骤4：跨帧建立并更新定位假设

定位模块将当前产生的候选对齐关系，与前几次恢复搜索形成的定位假设进行关联。

如果连续多次搜索均在相近位置和朝向得到候选，则相应定位假设的支持程度增加。

如果某一定位假设在后续搜索中不再得到支持，则增加其连续未关联次数。

该步骤的输出包括：

- 当前获得最多连续支持的定位假设；
- 排名第二的定位假设；
- 两者之间的质量差异；
- 各定位假设的连续支持情况。

定位模块不得因为某一次搜索中某个候选的代价最低，就直接把移动终端跳转到该候选位置。

### 步骤5：判断定位恢复是否收敛

这里的“收敛”不是指移动终端收敛，也不是指货架收敛。

通俗地说，定位恢复收敛是指：

> 连续几次有效搜索逐渐指向同一个定位答案，并且其他竞争答案已经明显不如该答案可信。

具体而言，定位模块检查：

- 是否连续多次支持同一个定位假设；
- 当前结构点是否充分；
- 匹配代价是否合格；
- 最佳定位假设是否明显优于次佳定位假设；
- 候选对齐关系是否在连续搜索中逐渐稳定；
- 候选要求的校正幅度是否符合设备可能的实际运动；
- 当前传感器跟踪状态是否可以支持校正。

本节所称的“收敛”，是“在线定位假设和地图—连续里程计对齐关系的收敛”。

它不是扫描结束后整个SLAM图优化的收敛。后者属于后续扫描后处理步骤，应当另行描述。

### 步骤6：记录证据并采用恢复结果

如果某一定位假设通过全部判断条件，定位模块先保存本次恢复过程及其判断证据，再将该定位假设标记为“已接受定位结果”。

随后，定位模块按照2.6的有界渐进校正方法，小步更新地图—连续里程计对齐关系，而不是一次性跳到候选位置。

更新完成后，定位状态先回到“可用”。后续获得连续的局部正常匹配证据后，再进入“稳定”。

### 步骤7：处理未恢复成功的情况

如果达到截止时间或者有效搜索次数上限后，仍然存在以下情况：

- 最佳和次佳定位假设无法区分；
- 没有定位假设获得连续支持；
- 当前结构不足；
- 候选要求的校正幅度不安全；

则本次定位恢复结束，但不采用任何新位置。

定位模块继续保持弱可信或者定位丢失状态，并：

- 保留原始连续里程计轨迹；
- 保留已识别的价签内容和原始测量；
- 保留恢复过程中产生的候选和拒绝原因；
- 停止自动更新地图—连续里程计对齐关系；
- 停止发布无法证明的价签地图坐标；
- 提示补充扫描或者进行人工校正。

## 七、“中间候选”与“扫描后绝对位姿约束”是什么

原文中的“中间候选”含义不够明确，建议统一改为：

> 待确认候选对齐关系。

它是定位恢复过程中搜索得到、但尚未通过连续支持、唯一性和安全性判断的候选结果。

待确认候选只能用于：

- 后续定位假设跟踪；
- 与其他候选进行比较；
- 记录定位恢复的处理过程；
- 分析定位恢复失败原因。

待确认候选不能直接修改正式地图定位，也不能直接用于计算正式价签地图坐标。

原文中的“正式后处理绝对约束”建议统一改为：

> 扫描后绝对位姿约束。

扫描后绝对位姿约束是指：定位结果已经通过可信判断，并且已经绑定到某一个确定的同步定位与建图（SLAM）节点后，形成的“该节点在门店先验地图中应位于何处”的约束信息。

它至少说明：

- 对应哪个SLAM节点；
- 该节点在门店先验地图中的位置和朝向；
- 该定位结果已经被接受；
- 该定位结果的质量或不确定程度；
- 该结果来自哪一次定位观测或定位恢复。

只有“已接受定位结果”才能进一步形成扫描后绝对位姿约束。待确认候选和未成功的定位恢复结果只能保存为过程证据，不能进入正式图优化。

## 八、图6英文词语的统一中文含义

图6建议直接改成中文。必须保留英文时，可采用以下对应关系：

- `uninitialized`：未初始化；
- `initializing`：初始化中；
- `usable`：可用；
- `stable`：稳定；
- `weak`：弱可信；
- `lost`：定位丢失；
- `recovering`：恢复中；
- `Recovery Episode`：一次定位恢复回合；
- `deadline`：截止时间；
- `cooldown`：冷却期；
- `provisional`：待确认；
- `accepted`：已接受；
- `provisional ≠ accepted`：待确认不等于已接受；
- `Local`：局部正常匹配；
- `manualCorrection`：人工校正。

图6标题建议改成：

> 图6 定位状态转换与定位恢复流程

此前回答文档中还列出了“恢复触发”“扩大范围搜索”“候选区域”“定位假设跟踪”等概念。这些概念可以用于解释处理步骤，但它们并不是当前图6中实际显示的全部英文框名，不建议突然塞入图6术语表，以免代理人误以为又出现了新的模块或数据对象。

## 九、图6的数据流向

图6的数据流可以统一解释为：

```
加载门店先验地图和初始位置
        ↓
未初始化
        ↓
初始化中
        ↓
获得一个通过基本判断的地图定位结果
        ↓
可用
        ↓
连续局部正常匹配支持同一定位结果
        ↓
稳定
        ↓
结构匹配、唯一性或跟踪质量下降
        ↓
弱可信
   ↙            ↘
恢复搜索成功      持续无法定位
   ↓              ↓
可用          定位丢失
   ↑              ↓
   └────定位恢复───┘
```

定位恢复成功后必须先回到可用，再经过新的局部正常匹配进入稳定。定位恢复没有成功时，保持弱可信或定位丢失，不能输出无法证明的价签地图坐标。

## 十、结合平行货架场景说明

例如，移动终端原来稳定定位在第三条货架通道。由于顾客遮挡或者设备快速转弯，当前结构点与门店先验地图的匹配质量下降，定位状态进入弱可信。

此时移动终端仍然继续保存连续里程计轨迹和识别到的价签，但不再直接发布新价签的地图坐标。

如果普通局部匹配不能恢复，定位模块启动一次定位恢复回合。扩大搜索后，系统在第三条和第四条货架通道分别找到一个候选对齐关系。

后续连续搜索中：

- 第三条通道对应的候选持续出现，位置和朝向逐渐稳定；
- 第四条通道对应的候选只偶尔出现，或者匹配质量不稳定。

定位模块据此判断第三条通道对应的定位假设收敛，将其标记为已接受定位结果，并小步更新地图—连续里程计对齐关系。

更新后，定位状态先回到可用。经过后续连续局部正常匹配验证后，再进入稳定，恢复价签自动定位。

如果两个通道的候选始终难以区分，定位模块不选择其中任何一个，而是保持弱可信或定位丢失，继续保留原始数据并要求补充扫描或人工校正。


## 2.8 价签自动识别 深度测量与原始观测保存

### 2.8.1 本部分解决的问题

本部分说明移动终端在连续扫描门店的过程中，怎样自动读出价签上的条码，怎样测得价签相对于相机的三维位置，以及怎样把这次识别和测量与采集时间、扫描节点、先验地图及扫描会话准确对应并保存。

这里需要先明确一个边界：移动终端读出条码，只能说明它识别到了某个价签；移动终端测得一个三维点，也只能说明它在当前采集时刻得到了一次空间测量。这两项结果都不当然等于“已经得到可靠的最终价签地图位置”。最终位置还取决于移动终端当时在门店先验地图中的定位是否可信、观测是否绑定到正确的扫描节点，以及扫描结束后该节点的位置和朝向是否经过修正。

因此，本部分采用的基本思路是：先把每次采集时能够确认的原始事实完整保存，再在后续处理中根据修正后的扫描轨迹重新计算价签位置。这样可以避免在线定位的临时误差被永久写入价签结果。

本部分承接2.7的定位结果，并向2.9和后续扫描后处理提供输入：

```text
2.7：给出移动终端当前的先验地图定位状态和定位结果
  ↓
2.8：识别价签条码，测量价签三维位置，绑定扫描节点并保存原始观测
  ↓
2.9：将同一实体价签的多次独立观测组成价签多帧观测组并检查质量
  ↓
扫描后处理：使用修正后的扫描节点位姿重新计算价签最终地图位置
```

### 2.8.2 执行主体和统一用词

本部分的执行主体是移动终端，例如安装MarketScanner的手机。条码识别、深度取样、深度质量判断、三维点计算、扫描节点绑定和原始观测保存，均由移动终端完成。

为避免同一对象在前后文中使用不同名称，本部分统一采用以下用词：

| 原词或英文名称 | 统一中文名称 | 本部分中的含义 |
| --- | --- | --- |
| payload或payloadStringValue | 条码内容 | 从二维码或条形码中解码得到的字符串，例如TAG0007。它不是价签坐标，也不一定直接表示商品价格。 |
| symbology | 条码制式 | 条码采用的编码种类，例如二维码QR、EAN-13或Code 128。 |
| bounding box或normalized bounds | 条码区域 | 条码在相机图像中的矩形范围。采用归一化表示时，位置和宽高用0到1之间的比例记录。 |
| ARFrame | 采集帧 | 移动终端在同一采样时刻取得的一组数据，包括相机图像、可用深度、相机内参、相机位姿和设备跟踪状态。 |
| frame ID或frameID | 帧编号 | 在一次扫描会话内标识某一个采集帧的编号。不同采集帧必须使用不同的帧编号。 |
| observation ID | 观测编号 | 标识某一条价签观测的编号。一帧识别到两个价签时，两条观测可以具有相同的帧编号，但必须具有不同的观测编号。 |
| SLAM node | 扫描节点 | 同步定位与建图系统从连续采集数据中保存的一个带编号的位置和朝向记录。它是扫描轨迹中的记录点，不是货架上的点。 |
| boundNodeID | 绑定节点编号 | 表示某一条价签观测对应哪个扫描节点。扫描后根据该编号查找同一节点的修正后位姿。 |
| node stamp | 节点时间 | 扫描节点自身记录的时间。它与采集帧的帧时间分别保存，并通过时间差检查确认能够对应。 |
| tracking session | 跟踪会话 | 移动终端一次连续的设备跟踪过程。不同跟踪会话中的采集帧、扫描节点和坐标不能混用。 |

本部分统一使用“先验地图”表示扫描前已经存在的门店地图，使用“扫描地图”表示本次扫描过程中形成的地图。先验地图编号用于说明采用了哪张门店地图；扫描地图编号用于说明扫描节点属于本次扫描地图中的哪一部分。二者不是同一个编号，正文中不能只写含义不清的“地图ID”。

### 2.8.3 采集帧 扫描节点和价签观测的关系

移动终端相机会连续产生采集帧，但同步定位与建图系统不一定为每个采集帧都建立一个扫描节点。它可以从连续采集帧中选择部分数据形成节点。因此：

- 采集帧是相机及相关传感器在某一时刻取得的同步观测；
- 扫描节点是同步定位与建图系统保存的轨迹记录点；
- 价签观测是移动终端在一个采集帧中，对一个价签形成的一次识别、测量和关联记录；
- 采集帧和扫描节点不是一一对应关系，多个采集帧可以绑定到同一个扫描节点，也可以分别绑定到不同节点。

条码识别通常需要一定的计算时间。移动终端可以在取得采集帧后异步完成识别，但识别结果必须继续使用原采集帧对应的图像、深度、相机内参、相机位姿、设备跟踪状态和帧时间，不能在识别完成时临时换成最新采集帧的数据。

这项要求可以概括为“同一采集帧对应”：一条价签观测中用于识别、深度测量和空间换算的数据，都应当对应同一个采集时刻。否则，前一帧的条码区域可能被错误地配上后一帧的深度或相机位姿，移动终端在行走时就会计算出错误的价签位置。

### 2.8.4 移动终端怎样自动识别价签条码

移动终端直接使用连续扫描所采用的同一相机采集流，不另行启动第二个相机采集会话。它从采集帧中取得相机图像，并调用Apple Vision，也就是苹果图像识别框架中的条码检测功能。

具体处理过程如下：

1. 移动终端取得一个采集帧及其帧编号。
2. 移动终端在屏幕指定的识别区域内查找二维码、条形码或者其他机器可读标识。
3. 如果指定区域内没有得到可靠结果，可以扩大到邻近区域再尝试一次，以兼顾操作方便和识别速度。
4. 对每个识别结果，移动终端保存条码内容、条码制式和条码区域，并为该结果建立观测编号。
5. 该观测继续使用原采集帧对应的深度、相机参数、相机位姿和时间，进入后续三维测量。

例如，移动终端从第1001采集帧中读出字符串TAG0007，并识别其制式为EAN-13，同时得到该条码在图像中的矩形区域。此时系统只能确认“第1001采集帧中识别到了条码内容为TAG0007的价签”，还不能确认该价签在门店地图中的最终位置。

### 2.8.5 怎样在条码区域中采集和过滤深度

深度表示相机看到的某个图像位置离相机有多深。条码区域可能同时包含价签表面、后方货架、标签边缘和前方遮挡物。如果只读取一个像素，容易刚好读到深度空洞、反光区域或背景货架。因此，移动终端在条码区域内读取多个深度值，再从中寻找能够代表同一表面的深度组。

#### 步骤1 在条码区域内均匀取样

本步骤的输入包括条码区域，以及同一采集帧对应的深度图。移动终端先将条码区域的四边向内收缩，减少条码边缘之外的背景混入，再在收缩后的区域内均匀布置采样点。

参考实施例按宽度和高度各向内收缩18%，再布置9×9共81个采样点。如果相机图像和深度图的尺寸不同，移动终端需要把条码区域从图像坐标换算到深度图中的对应位置，不能直接用原图像像素读取深度。

本步骤输出81个待检查的深度采样位置及其深度值。81只是参考实施例的取样数量，不是本发明只能采用的数量。

#### 步骤2 删除不能使用的深度值

移动终端先排除不能表示为有效有限数值的深度，再排除小于0.2米或者大于8米的深度。过近或过远的值超出该实施例认可的工作范围，不能参与后续判断。

如果采集帧同时提供与深度图正确对应的深度置信度图，移动终端还排除置信度值为0的采样点，也就是传感器标记为最低置信等级的采样点。其余非零置信等级在本实施例中都可以继续参加后续统计判断。

深度置信度图可能存在三种情况：

- 深度置信度图存在，并且尺寸、数据格式和内存布局均与深度图正确对应时，移动终端按置信度值过滤采样点；
- 深度置信度图不存在时，移动终端跳过置信度筛选，但仍执行深度范围和后续统计检查；
- 深度置信度图存在，但尺寸、格式或数据访问状态与深度图不匹配时，该采集帧的深度测量作废，不能假装成有效测量。

经过上述处理后，移动终端得到“有效深度样本”。有效深度样本只是通过初步过滤的候选值，还不能直接认定为价签表面。

### 2.8.6 怎样从混合深度中找出同一表面

本部分所称的“一致表面簇”，统一解释为“同一表面的深度组”，即一组深度相近、可能来自价签所在表面的采样值。移动终端按以下过程寻找该深度组。

#### 步骤1 形成候选深度组

移动终端把全部有效深度样本从近到远排序，并在排序结果中查找深度跨度不超过0.10米、且至少包含12个样本的连续区间。每个满足条件的区间都可以看作一个候选深度组。

如果存在多个候选深度组，移动终端优先选择距离相机较近的一组；当两个候选深度组的代表深度相差不超过0.02米时，优先选择包含样本较多的一组。这样既考虑了遮挡关系，也避免在两个深度几乎相同的组之间仅凭微小数值差选择样本更少的一组。

#### 步骤2 检查所选深度组是否能够代表整体观测

移动终端事先计算全部有效深度样本的中位数。选出候选深度组后，再检查该整体中位数是否位于所选深度组的范围内。

中位数是把全部数值从小到大排列后取得的中间值。与简单平均值相比，中位数不容易被少量特别大或特别小的异常值拉偏。

如果所选深度组不包含全部有效深度样本的中位数，说明较近的一组可能只是少量前景遮挡物，或者图像中存在两个无法可靠区分的前后表面。此时移动终端把失败原因记录为“深度层存在歧义”，并拒绝把该组直接当作价签深度。当前参考实现不会在拒绝较近组后，再悄悄改选另一个较远组。

例如，条码区域中的有效深度主要形成约1米和约2米两组。移动终端虽然先选中了约1米的近处组，但全部有效样本的中位数约为2米，说明约1米处更可能只是局部遮挡物。移动终端应拒绝本次深度选择，不能因为近处组存在若干数值就把它当作价签表面。

### 2.8.7 怎样用中位数 MAD和内点比例判断深度质量

所选深度组通过整体中位数检查后，移动终端还要判断该组内部是否足够集中，以及它在全部有效深度样本中是否占据足够比例。这里使用中位数、中位绝对偏差和内点比例三个指标。

中位绝对偏差的英文名称为Median Absolute Deviation，缩写为MAD。其计算过程是：先计算所选深度组中每个样本与该组中位数相差多少，再对这些绝对差值取中位数。MAD越小，表示样本越集中在同一个深度附近。

“内点”是指与所选深度组中位数之差没有超过允许偏差的样本。参考实施例把“0.02米和3倍MAD中的较大值”作为允许偏差：

$\text{允许偏差}=\max(0.02\text{米},3\times MAD)$

“内点比例”是内点数除以初步过滤后的全部有效深度样本数。其分母不是最初的81个取样点，也不是只计算所选深度组中的样本数。这样计算可以判断所选表面在整个条码区域中是否占有足够的深度证据。

参考实施例同时要求：

- 内点不少于12个；
- 内点数占全部有效深度样本的比例不低于55%；
- 所选深度组的MAD不大于0.045米；
- 所选深度组已经通过前述整体中位数检查。

上述条件全部满足时，深度统计检查才通过。本步骤输出所选深度组的代表深度、有效样本数、内点数、内点比例、MAD及检查结果。这些数据既供后续三维点和平面检查使用，也作为本次价签观测的深度质量记录保存。

例如，81个采样点经过初步过滤后剩下60个有效深度样本，其中40个集中在1.18米至1.22米，另外20个来自较远背景。假设所选深度组的中位数为1.20米，MAD为0.01米，并且已经通过整体中位数检查，则允许偏差为0.02米和3×0.01米中的较大值，即0.03米。位于1.17米至1.23米范围内的40个样本成为内点，内点比例为40÷60，约为66.7%。内点数、内点比例和MAD均满足上述参考条件，因此深度统计检查通过。

如果同样有60个有效深度样本，但只有25个内点，内点比例只有约41.7%。即使这25个样本彼此接近，也不能通过55%的参考条件，移动终端不能据此声称已经测准价签位置。

参考实现为后续三维点和平面计算重新筛选样本时，允许偏差采用“0.025米和3倍MAD中的较大值”。这里的0.025米用于三维点和平面计算，前面的0.02米用于深度统计检查，二者用途不同，不能合并为一个含义不明的阈值。

### 2.8.8 怎样把图像位置和深度换算为价签三维点

深度统计检查通过后，移动终端利用像素位置、可信深度和同一采集帧的相机内参，计算价签在相机坐标中的三维位置。

对于像素位置\((u,v)\)、可信深度\(z\)和相机内参矩阵\(K\)，价签相机坐标可以表示为：

$p^{camera}_{tag}=zK^{-1}[u\ v\ 1]^T$

其中：

- $p^{camera}_{tag}$ 表示价签在相机坐标中的三维点；
- 上标 $camera$ 表示该点使用相机坐标；
- 下标 $tag$ 表示该点对应价签；
- $K^{-1}$ 表示相机内参矩阵的逆；
- $[u\ v\ 1]^T$ 表示把图像中的像素位置写成一个列向量；
- $z$ 表示该像素方向上的可信深度，应当按照相机深度模型的定义使用，不能随意替换成另一种距离。

通俗地说，相机内参用于确定图像中的这个像素朝向空间中的哪个方向，可信深度用于确定沿该方向走多远。二者结合后，才能得到价签相对于相机的位置。

参考实现不是把全部三维内点简单取平均作为价签点，而是从保留样本中选择深度最接近所选深度组中位数的三维点，作为本次测得的价签三维点。这样可以使价签点与前述稳健深度中心保持一致。

### 2.8.9 怎样检查价签附近是否形成可信局部表面

移动终端还要检查筛选后的三维点是否大致落在同一个局部平面上。这里的“局部平面”是价签区域附近的一小块表面，不是重新生成整张货架地图。

当前参考实施例采用代表点构造近似局部平面，具体过程如下：

1. 移动终端按照样本在图像中的位置，分别选出横向最左、横向最右、纵向最上和纵向最下的代表点。
2. 最左点到最右点形成横向向量，最上点到最下点形成纵向向量。
3. 移动终端对横向向量和纵向向量做叉乘，得到同时垂直于两个向量的方向，再把该方向的长度归一为1。这个垂直方向称为“表面法向”。
4. 移动终端计算筛选后三维点的平均位置，得到这些点的质心。
5. 让局部平面经过该质心，并以表面法向作为平面的垂直方向，从而确定近似局部平面。

表面法向可以理解为垂直插在价签表面上的一根短箭头，用于描述表面的朝向。移动终端首先检查表面法向能否正常计算，包括各数值是否为有效有限值、叉乘结果的长度是否接近零。如果两个方向向量近似平行或者样本分布退化，就无法得到可靠的表面法向。

表面法向有效时，移动终端计算每个三维点到该平面的绝对距离，再对这些距离取中位数，得到“平面残差”。平面残差越小，说明多数三维点越贴近同一个平面。参考实施例要求平面残差中位数不超过约0.06米。

例如，平面残差中位数为0.01米，表示一半以上的采样点到该平面的距离不超过约1厘米，局部表面较为一致。如果平面残差中位数为0.10米，就超过了上述参考条件，不能把本次表面检查判定为通过。

如果表面法向无效或者平面残差不合格，移动终端记录“深度表面不稳定”，不将本次场景深度测量标为可靠位置。如果已经知道价签所在货架平面，并且相机观察方向能够与该平面形成有效交点，移动终端可以改用“货架平面求交”方法计算位置；如果也不具备这一条件，则将测量方法记录为“无法形成可用位置”。条码内容和失败原因仍然可以保留。

这里需要区分两个位置：价签三维点使用“深度最接近所选深度组中位数的保留样本”；局部平面的基准位置使用“全部保留三维点的质心”。质心仅用于平面一致性检查，不能与价签三维点混为一谈。

上述方法属于利用代表点构造近似局部平面。正文不应写成已经采用最小二乘平面拟合或者随机抽样一致性平面拟合，也不能把“原始深度仍被保存”写成“深度定位已经成功”。

### 2.8.10 测量方法 深度质量和定位状态分别表示什么

一条价签观测需要分别记录“怎样得到三维位置”“深度测量本身是否可靠”以及“移动终端当时在先验地图中定位得是否可靠”。三者评价不同对象，不能互相替代。

#### 测量方法

“测量方法”表示价签三维位置采用哪种方式得到。参考实现保存以下取值：

| 保存取值 | 统一中文含义 |
| --- | --- |
| smoothed_scene_depth | 平滑场景深度，即利用经过平滑处理的场景深度计算价签三维位置。 |
| scene_depth | 原始场景深度，即利用未经上述平滑处理的场景深度计算价签三维位置。 |
| shelf_plane_ray | 货架平面求交，即计算相机穿过条码位置的观察方向与已知货架平面的交点。 |
| unavailable | 无法形成可用位置，即当前没有满足要求的三维测量结果。 |

程序内部的withPlane只表示某个处理方法或阶段，不是独立的保存取值；正文不应把它列成一种新的测量方法。

#### 深度质量和测量置信度

“深度质量”记录深度测量的具体依据，包括有效样本数、内点数、内点比例、MAD、平面残差和表面法向等。plane_residual_m表示以米记录的平面残差；surface_normal_camera表示在相机坐标中记录的表面法向。

“测量置信度”表示移动终端对本次三维测量可靠程度的评价。它根据采用的测量方法和质量证据形成，不是条码识别置信度，也不是移动终端的地图定位置信度。表面检查失败时，移动终端记录unstable_depth_plane，中文含义为“深度表面不稳定”，并按实际情况改用货架平面求交或者记录为无法形成可用位置。

#### 先验地图定位状态和设备跟踪状态

“先验地图定位状态”表示移动终端当前与先验地图的对应关系是否可靠。其状态可以包括未初始化、初始化中、可用、稳定、弱可信、定位丢失、恢复中和人工校正中。只有定位状态满足后续发布条件时，采集时计算的地图位置才可能用于自动价签定位。

“设备跟踪状态”表示ARKit对相机和惯性传感器跟踪情况的判断。normal表示正常，limited表示受限，notAvailable表示不可用。设备跟踪正常，只能说明传感器跟踪可用；移动终端仍可能尚未在先验地图上找准位置。因此，设备跟踪状态和先验地图定位状态必须分别保存。

### 2.8.11 怎样把价签观测绑定到准确的扫描节点

节点绑定的目的，是记住“这次价签测量对应哪个扫描节点，以及价签相对于该节点位于什么位置”。扫描结束后，即使该节点在扫描地图中的位置和朝向被优化，系统仍能找到同一个节点，并根据原来保存的相对关系重新计算价签位置。

节点绑定按照以下过程执行。

#### 步骤1 保存原采集帧的数据

移动终端保存该采集帧的帧编号、帧时间、相机位姿、相机内参、设备跟踪状态、先验地图定位状态，以及本次条码识别和三维测量结果。条码识别可以异步完成，但不能在识别完成时改用另一采集帧的数据。

#### 步骤2 取得一个内容相互一致的扫描节点快照

移动终端从同一次节点读取结果中，同时取得绑定节点编号、节点时间、节点所属的扫描地图编号，以及该节点的位置和朝向。将这些数据作为一个整体读取，可以避免节点编号已经更新，而节点时间或节点位姿仍来自旧节点。

采集帧和扫描节点不必一一对应，帧时间和节点时间也不要求完全相等，但二者必须足够接近，才能证明该节点与本次观测对应。参考实现同时检查采集帧与节点快照的时间差，以及节点时间经过时钟换算后与帧时间的差，允许的时间差不超过1秒。超过允许范围时，不得把该节点强行绑定到价签观测。

#### 步骤3 把价签位置换算为相对绑定节点的位置

移动终端先利用采集帧中的相机位姿，把价签相机坐标换算到本次扫描采用的空间坐标中；再利用绑定节点的位置和朝向，把该点换算为“价签相对绑定节点的位置”。

若用$T_{W\leftarrow C}$表示相机坐标到扫描空间坐标的变换，用$T_{W\leftarrow N}$表示绑定节点坐标到扫描空间坐标的变换，则可以写成：

$p_W=T_{W\leftarrow C}p^{camera}_{tag}$

$p_N=\left(T_{W\leftarrow N}\right)^{-1}p_W$

其中，\(p_W\)表示采集时价签在扫描空间中的位置，\(p_N\)表示价签相对于绑定节点的位置。该换算同时包含方向变换和平移，不能在相机或者节点发生旋转时只做坐标相减。

通俗地说，移动终端保存的是“价签位于节点208的什么方向、离节点208多远”。这种相对关系不会因为扫描后节点208在地图中的位置被修正而失效。

#### 步骤4 保存绑定身份

移动终端至少保存绑定节点编号、节点时间和节点所属的扫描地图编号，并把这些字段与帧编号、观测编号、先验地图编号及摘要、楼层和跟踪会话一同写入价签观测。后续处理根据绑定节点编号直接查找同一节点，再核对节点时间等身份信息，不重新猜测哪个节点在时间上最接近。

### 2.8.12 原始观测保存哪些内容

本部分所称的“原始观测保存”或“原始观测冻结”，是指把采集当时已经确定的事实固定记录下来。它不表示停止相机采集，也不表示后续不能重新计算价签位置；它表示后续校正不得悄悄覆盖原始测量和原始身份。

每条价签观测至少保存以下内容：

| 信息类别 | 保存内容 | 作用 |
| --- | --- | --- |
| 观测身份 | 观测编号、帧编号 | 区分不同观测，并防止把同一采集帧重复计算为多个独立帧。 |
| 扫描节点身份 | 绑定节点编号、节点时间、扫描地图编号 | 确认本次观测对应哪个扫描节点，并供扫描后查找同一节点。 |
| 先验地图身份 | 先验地图编号、先验地图摘要、楼层 | 确认本次观测采用了哪一张门店地图及其具体内容版本，防止跨地图或跨楼层混用。 |
| 扫描身份 | 扫描会话编号、跟踪会话 | 防止把不同扫描过程或者不同设备跟踪过程的数据混在一起。 |
| 条码识别 | 条码内容、条码制式、条码区域 | 说明识别到了什么条码，以及条码在原采集图像中的位置。 |
| 采集时间和状态 | 帧时间、设备跟踪状态、先验地图定位状态和定位置信度 | 说明本次观测发生在何时，以及当时的设备跟踪和门店地图定位是否可信。 |
| 三维测量 | 价签相机坐标、价签相对绑定节点的位置、采集时的价签地图坐标 | 保存原始空间测量、节点相对关系和采集时的在线地图结果。 |
| 测量质量 | 测量方法、测量置信度、有效样本数、内点数、内点比例、MAD、平面残差、表面法向及失败原因 | 说明三维位置是怎样得到的，以及该结果有哪些质量依据。 |

先验地图摘要中的SHA是Secure Hash Algorithm的英文缩写，中文可称为“安全散列算法”。这里保存的是根据先验地图文件内容计算出的摘要值，用于核对具体地图版本。先验地图编号相同，并不能单独证明两份地图文件的内容完全相同，因此还需要保存并核对摘要。

“价签相机坐标”表示采集时价签相对于相机的三维位置。“价签相对绑定节点的位置”表示价签与绑定节点之间的相对关系。“采集时的价签地图坐标”表示按照采集当时的在线定位结果换算出的地图位置，可以用于即时显示和审计，但不当然等于最终位置。

### 2.8.13 扫描后怎样根据绑定节点重新计算价签位置

扫描过程中得到的节点位置可能含有累计误差。扫描结束后，图优化可以修正扫描节点在地图中的位置和朝向，但不改写价签观测中已经保存的原始事实。

后续处理根据绑定节点编号查找该节点的修正后位姿，并利用原来保存的“价签相对绑定节点的位置”重新计算价签地图位置。若用 $T^{final}_{M\leftarrow N}$ 表示绑定节点在门店先验地图中的修正后位姿，则价签最终地图位置可以表示为：

$p^{final}_{M}=T^{final}_{M\leftarrow N}p_N$

其中，$p_N$始终是采集时保存的价签相对绑定节点的位置。后续处理更新的是节点在门店先验地图中的最终位姿，以及由此计算出的价签结果，而不是覆盖原始相机测量或者把原观测改成另一条记录。

例如，一条观测在采集时绑定到节点208。扫描结束后，图优化把节点208的位置向东修正0.12米，并把其朝向修正2度。后续处理仍按绑定节点编号找到节点208，再把原来保存的价签节点相对位置应用到节点208的修正后位姿上。这样得到的价签地图位置会随正确的节点修正而更新，同时原采集帧、原深度和原绑定关系保持不变。

如果找不到绑定节点、节点时间或扫描地图身份不一致、坐标框架无法解释，后续处理不得改用“时间最接近的其他节点”来猜测位置。此时应保留条码内容和原始证据，但关闭该价签地图坐标的自动发布。

### 2.8.14 图7中的多帧是什么意思

图7中的“多帧”是指同一次移动终端扫描中，相机连续取得的多个采集帧。这些帧来自连续扫描所使用的同一相机采集流，每一帧都可以独立完成条码识别、深度测量和扫描节点绑定。

它不是以下含义：

- 不是扫描结束后从同步定位与建图点云中任意抽取的多组点；
- 不是要求每一帧都必须形成新的扫描节点；
- 不是把同一张图像重复处理多次后，伪装成多个独立采集帧。

价签多帧观测组的英文为Tag Burst。本文统一称为“价签多帧观测组”，不使用容易造成误解的“爆发”或“突发”等直译。它表示移动终端在一个短时间窗口内，围绕同一实体价签取得的多次独立观测。

例如：

| 采集帧 | 观测编号 | 绑定节点 | 条码内容 |
| --- | --- | --- | --- |
| 第1001采集帧 | 观测A | 节点208 | TAG0007 |
| 第1002采集帧 | 观测B | 节点208 | TAG0007 |
| 第1003采集帧 | 观测C | 节点209 | TAG0007 |

上述三次观测可以组成一个价签多帧观测组。前两帧绑定同一个扫描节点，第三帧绑定另一个扫描节点，并不妨碍它们属于同一组。移动终端需要确认这些观测对应同一实体价签，并检查条码内容、条码制式、先验地图版本、楼层和跟踪会话是否一致。参考实施例还要求相邻观测的时间间隔在0秒至3秒之间；时间倒退或者超过3秒时，结束当前观测组并建立新组。

条码内容相同只是身份核对的一部分。门店不同位置可能存在内容相同的商品条码，因此不能只凭条码内容相同，就把两个位置不同的实体价签合并成同一个价签多帧观测组。组建时还需要结合连续观察过程、时间关系和其他身份字段进行判断。

每个有效采集帧必须具有唯一的帧编号，每条价签观测必须具有唯一的观测编号。重复处理同一张图像不能增加有效帧数。一个观测组是否完整、是否具有足够独立帧数、是否允许进入自动接受流程，由2.9进一步判断；本部分只负责为每一帧形成内容完整、身份明确、可以复核的原始价签观测。

### 2.8.15 完整数据处理流程

本部分的数据处理过程可以统一表示为：

```text
步骤S281：从同一相机采集流取得采集帧、帧编号和帧时间
        ↓
步骤S282：识别条码，得到条码内容、条码制式和条码区域
        ↓
步骤S283：在同一采集帧的条码区域内读取多个深度样本
        ↓
步骤S284：排除无效深度，并形成一个或多个候选深度组
        ↓
步骤S285：根据整体中位数、MAD、内点数和内点比例
          判断所选深度组是否可信
        ↓
步骤S286：利用像素位置、可信深度和相机内参
          计算价签相机坐标
        ↓
步骤S287：构造近似局部平面，检查表面法向和平面残差
        ↓
    ┌────────深度和平面检查通过────────┐
    ↓                               │
使用场景深度测量                       │
                                    │
    ┌────────深度或平面检查未通过──────┘
    ↓
若存在可用货架平面，则采用货架平面求交；
否则记录无法形成可用位置
        ↓
步骤S288：取得与原采集帧对应的扫描节点快照
        ↓
步骤S289：把价签位置换算为相对绑定节点的位置
        ↓
步骤S2810：保存观测身份、地图身份、扫描身份、
           原始测量、定位状态和质量证据
        ↓
输出：一条已固定保存并绑定准确扫描节点的价签观测
        ↓
2.9：与同一实体价签的其他独立观测组成价签多帧观测组
```

### 2.8.16 结合一次连续扫描说明

假设工作人员手持移动终端沿货架行走。第1001采集帧中，移动终端识别到条码内容TAG0007，并得到条码区域。移动终端在该区域内布置81个深度采样点，初步过滤后剩下60个有效深度样本，其中40个集中在1.18米至1.22米。

移动终端选出这一深度组，检查整体中位数位于组内，再计算该组中位数、MAD和内点比例。假设中位数为1.20米、MAD为0.01米、内点比例约为66.7%，则本次深度统计检查通过。移动终端利用条码像素位置、1.20米附近的可信深度和相机内参，计算价签相机坐标；随后构造近似局部平面，并确认表面法向和平面残差满足参考条件。

同一采集时刻，移动终端取得扫描节点208的完整快照。移动终端把价签相机坐标换算为价签相对节点208的位置，再将观测编号、帧编号、节点208的编号和节点时间、先验地图编号和摘要、楼层、跟踪会话、定位状态、测量方法和深度质量一同保存。

移动终端继续行走时，第1002采集帧可能仍绑定节点208，第1003采集帧可能绑定节点209。只要三次观测对应同一实体价签，并满足时间和身份条件，就可以在2.9中组成一个价签多帧观测组。三个采集帧不要求对应三个不同扫描节点。

扫描结束后，如果图优化修正了节点208和节点209的位置，后续处理根据每条观测的绑定节点编号，分别取得相应节点的修正后位姿，再重新计算价签地图位置。采集时的条码内容、深度、节点绑定和质量证据保持不变。

如果第1001采集帧的深度表面不稳定，移动终端可以保留TAG0007及失败原因，但不能把该深度点当作可靠位置。如果移动终端当时的先验地图定位处于弱可信、定位丢失或者恢复中，即使条码和深度测量成功，也不能直接发布缺乏地图定位依据的价签坐标。这种处理保证了原始观测可以继续留存，同时避免不可靠结果进入正式价签地图。

上述取样数量、距离范围、时间差和质量阈值用于说明一个可实施的参考方案。不同设备或者门店场景可以采用经过验证的其他数值。本部分需要保持的核心处理关系是：同一采集帧识别和测量、稳健筛选价签表面、绑定准确扫描节点、固定保存原始事实，并在扫描后根据修正后的节点位姿重新计算价签位置。
