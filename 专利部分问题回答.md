
## 2.6 重复货架环境下的多候选时序消歧与失败关闭

### 2.6.1 本部分解决的问题

在大型超市中，不同货架通道可能具有相近的宽度、朝向和结构排列。仅根据某一时刻的局部结构进行匹配时，真实通道和相邻的相似通道可能得到接近的匹配结果。

如果系统在每次匹配时都直接选择当前代价最小的候选位姿，就可能出现以下问题：

- 前一时刻定位在真实通道；
- 下一时刻因为局部遮挡、动态顾客或结构重复，另一通道的瞬时匹配代价略低；
- 系统立即切换到另一通道；
- 设备在门店地图中的位置发生不符合实际运动的横向跳变。

因此，本部分不依赖单帧最优结果直接校正地图位姿，而是同时保留多个候选，通过连续匹配更新建立若干“定位假设”，再结合跨帧支持程度、候选唯一性、匹配质量和运动连续性确定是否允许校正。

本部分承接2.5的输出：

```
2.5：在门店先验地图中产生若干候选位姿及其匹配质量
  ↓
2.6：对这些候选进行跨帧关联、时序消歧、可信判定和安全校正
```

### 2.6.2 “帧”和候选位姿的含义

本部分所称的“帧”，是指移动设备采集的一次同步观测，例如同一采样时刻的彩色图像、深度数据、相机内参、ARKit/VIO位姿和跟踪状态。

它不是专指SLAM数据库中的一个节点。SLAM系统可以从连续采集帧中选择部分帧形成SLAM节点，因此：

- AR采集帧是传感器观测；
- SLAM节点是由部分采集帧生成的图优化节点；
- 二者不能直接等同。

更准确地说，假设中的计数只在系统实际执行了一次结构匹配时更新。因此，原文中的“连续支持帧数”和“未命中帧数”，建议分别改为：

- 连续获得支持的有效匹配更新次数；
- 连续未关联的有效匹配更新次数。

这样可以避免让人误以为每一个ARFrame或者每一个SLAM节点都会使计数增加。

2.5输出的每个候选位姿，表示移动设备在门店先验地图坐标系 \(M\) 中的一种可能位置和朝向，即：

$T^{cand}_{M\leftarrow B}$

其中包括平面位置 \(x、y\)、偏航角 \(yaw\) 以及相应的匹配代价或质量分数。

系统再利用同一时刻连续里程计给出的设备位姿：

$T_{O\leftarrow B}$

将候选设备位姿转换为候选地图—里程计对齐关系：

$T^{cand}_{M\leftarrow O} = T^{cand}_{M\leftarrow B} \left(T_{O\leftarrow B}\right)^{-1}$

因此，2.6实际跟踪的是多个可能的“地图坐标系与连续里程计坐标系之间的对齐关系”，而不是跟踪某一片货架点云。

### 2.6.3 匹配代价、瞬时最小代价和质量分数

对于一个候选位姿，系统将当前局部结构点按照该候选位姿变换到门店先验地图中，并查询这些结构点与地图结构之间的距离。多个结构点的距离经过截断和汇总后形成候选匹配代价：

$J(T)=\frac{1}{N}\sum_{k=1}^{N} \min\left(D(Tp_k),d_r\right)^2$

其中：

- $p_k$为当前局部结构点；
- $T$为待评价的候选位姿；
- $D(\cdot)$为先验地图距离场查询；
- $d_r$为鲁棒截断距离；
- $J(T)$越小，表示当前观测与门店地图结构越一致。

几个容易混淆的概念应区分如下：

- “候选匹配代价”：某一个候选位姿对应的 $J(T)$；
- “瞬时最小代价”：本次搜索产生的全部候选中最小的 $J(T)$；
- “当前匹配代价”：当前被跟踪或评价的某一候选在本次更新中的代价；
- “平均匹配质量”：同一定位假设在连续多次有效更新中累积形成的质量；
- “质量分数”：匹配代价经过单调转换后的表达，通常代价越小，分数越高。

本发明不限于某一种具体的代价—分数换算公式。其核心是：既评价候选当前是否匹配，也评价该候选是否在连续观测中稳定成立。

### 2.6.4 定位假设是什么

“定位假设”是系统对地图—里程计对齐关系的一种跨帧解释。

例如，在两个外观和结构都很相似的平行货架通道中，单次搜索可能得到候选A和候选B。系统不会立即断定其中一个必然正确，而是建立两个定位假设：

```
定位假设A：设备位于货架通道A
定位假设B：设备位于货架通道B
```

每个定位假设至少维护：

- 所代表的候选地图—里程计对齐变换；
- 最近一次与该假设关联的候选位姿；
- 当前或累计匹配质量；
- 获得有效支持的次数；
- 连续未关联的有效匹配更新次数；
- 假设当前是否仍处于有效跟踪状态。

定位假设不是一帧数据，也不是一个货架点云，而是一条由连续匹配结果共同支持的候选定位轨迹。

原文中的“候选地图（点云地图）对里程计变换”容易误解，建议改成：

> 候选门店地图—连续里程计对齐变换。

此外，当前假设本身并不需要保存墙钟时间或每次命中的绝对时间戳。假设的新鲜程度可以通过“连续未关联更新次数”等状态表示。进入Recovery恢复过程后，恢复事件的开始、截止和结束时间可以单独记录，不应与定位假设字段混写。

### 2.6.5 新候选如何关联到已有定位假设

“新候选”是指2.5在当前一次匹配更新中产生、但尚未与历史定位假设关联的候选结果。

系统将新候选所代表的 $T^{cand}_{M\leftarrow O}$，与已有定位假设保存的对齐变换进行比较。比较内容主要包括：

- 平面位置差异；
- 偏航角差异；
- 必要时还可结合运动方向和位姿变化连续性。

如果新候选与某个已有假设在地图—里程计对齐空间中足够接近，则将其视为对该已有假设的一次新支持，并：

- 更新该假设的候选对齐关系；
- 累计或刷新支持信息；
- 更新综合匹配质量；
- 将连续未关联次数清零。

如果某个已有假设在本次有效匹配更新中没有关联到候选，则增加其连续未关联更新次数。当连续多次不能获得支持时，该假设可以转为非活跃状态或者被淘汰。

如果新候选与全部已有假设都不接近，则根据该候选建立一个新的定位假设。但新建立的假设只有初始证据，不能立即用于修正地图—里程计对齐关系。

因此，原文中的“同一候选”建议修改为：

> 属于同一定位假设的连续候选。

“同一定位假设”是根据候选所代表的地图对齐关系是否在空间和朝向上连续来判断的，并不是通过“是否来源于同一货架点云”判断的。即使两个货架形状相同，只要它们在地图中的位置不同，也应当形成不同的定位假设。

### 2.6.6 主定位假设如何确定

系统从当前仍然获得有效支持的定位假设中确定优先候选，而不是简单选择某一帧代价最小的结果。

主定位假设的判定可以综合考虑：

1. 是否在本次有效匹配更新中获得候选支持；
2. 最近连续有效更新中的支持程度；
3. 跨帧累计匹配质量；
4. 历史支持的稳定程度；
5. 与其他竞争假设之间是否存在足够的质量差异。

系统可以先按照连续支持程度和综合匹配质量进行排序，再比较排名第一和第二的定位假设。

但“排名第一”不等于“可以直接采用”。排名第一的假设还必须通过可信判定。如果其证据不足，系统不能绕过判定条件而任意选择排名第二的假设。

这种方式能够防止系统在两个相似货架之间仅因为一次微小的代价变化就发生来回切换。

### 2.6.7 可信判定使用哪些数据

定位假设是否可信，至少由以下几类可测量数据共同确定。

1. 结构证据是否充分

检查有效结构点数量，以及结构点在设备周围或视野中的方向覆盖程度。如果结构点过少或者只集中在单一方向，候选位姿容易沿着货架方向滑动，不足以支持可靠校正。

2. 匹配质量是否合格

检查候选匹配代价是否表明当前局部结构与门店地图结构具有足够的一致性。匹配代价过大，说明该候选不能合理解释当前观测。

3. 候选是否具有唯一性

比较最佳候选和次佳独立候选之间的代价或质量差异。如果两个候选结果过于接近，说明仅凭当前结构不能区分两个相似货架通道。

除单次候选差异外，还可以比较两个跨帧定位假设的累计质量差异，判断某个假设是否在时间上持续优于竞争假设。

4. 设备跟踪状态是否可靠

检查产生当前结构观测的同一采集帧中的ARKit/VIO跟踪状态。若跟踪丢失、受限或者姿态质量不足，则不应基于该帧结果修改地图对齐关系。

5. 候选校正幅度是否符合运动连续性

比较候选对齐变换与当前已采用对齐变换之间的平移和偏航角差异。如果候选要求设备瞬间跨越到另一条货架通道，或者产生明显不符合连续运动的旋转，则拒绝自动校正。

专利正文应保护上述多条件联合判定方法，而不必将具体点数、角度、距离和次数写成唯一限定值。具体阈值可作为优选实施例说明。

### 2.6.8 什么是“有界增益渐进更新”

本部分的“全局坐标”是指门店先验地图坐标系 \(M\) 下的业务位置和朝向，不是经纬度坐标，也不是ARKit内部的临时世界坐标。

可信定位假设通过判定后，系统也不必一次性把当前地图—里程计对齐关系直接替换为候选值，而是进行渐进更新：

$T^{new}_{M\leftarrow O} = \operatorname{Interp} \left( T^{old}_{M\leftarrow O}, T^{cand}_{M\leftarrow O}, g \right)$

其中：

- $T^{old}_{M\leftarrow O}$为当前使用的地图—里程计对齐关系；
- $T^{cand}_{M\leftarrow O}$ 为可信候选给出的对齐关系；
- $g$为更新增益；
- $\operatorname{Interp}$表示对平移量和偏航角进行渐进插值。

“有界”包含三层含义：

- 候选误差必须处于允许自动校正的范围内；
- 每次只吸收候选校正量的一部分；
- 每次实际执行的平移和偏航角修正不得超过单次安全上限。

更新后的设备地图位姿为：

$T^{new}_{M\leftarrow B} = T^{new}_{M\leftarrow O} T_{O\leftarrow B}$

这种处理只更新门店地图与连续里程计之间的对齐关系，不重置ARKit/VIO原始里程计，也不直接修改SLAM内部的原始轨迹。

### 2.6.9 失败关闭

如果出现下列任意情况：

- 有效结构点不足；
- 结构视角覆盖不足；
- 候选匹配代价过大；
- 最佳和次佳候选难以区分；
- 跨帧定位假设仍存在竞争；
- ARKit/VIO跟踪状态不可靠；
- 候选要求的校正幅度超过安全范围；

系统均不自动采用“看起来最接近”的货架通道。

此时系统：

1. 保留原有地图—里程计对齐关系；
2. 继续保留连续里程计轨迹；
3. 保存候选、代价、判定结果及拒绝原因；
4. 将定位可信状态标记为弱可信；
5. 若弱可信状态持续存在，则进入后续Recovery扩大搜索流程；
6. 在重新获得可信定位前，不输出缺乏证据支持的绝对门店坐标。

这里的“失败关闭”不是终止扫描或删除观测数据，而是关闭不可信的自动校正和坐标发布能力。

### 2.6.10 图5对应的完整数据处理流程

图5建议按照以下顺序绘制：

```
步骤S261：接收2.5输出的多个候选设备地图位姿及匹配代价
        ↓
步骤S262：利用同一时刻连续里程计位姿，
          将候选设备位姿转换为候选地图—里程计对齐变换
        ↓
步骤S263：按照平移和偏航角邻近关系，
          将当前候选关联到已有定位假设
        ↓
步骤S264：更新各假设的支持程度、综合匹配质量
          和连续未关联次数，并建立必要的新假设
        ↓
步骤S265：从当前获得支持的假设中确定最佳和次佳假设
        ↓
步骤S266：检查结构点数量、方向覆盖、匹配质量、
          候选唯一性、跟踪状态和校正幅度
        ↓
        ┌────────可信────────┐
        ↓                    │
步骤S267：采用有界增益渐进更新地图—里程计对齐关系
        ↓
输出更新后的门店地图位姿
                             │
        ┌──────不可信────────┘
        ↓
步骤S268：保持原对齐关系，记录拒绝证据并标记弱可信
        ↓
步骤S269：若不可信状态持续存在，进入Recovery扩大搜索
```

### 2.6.11 平行货架场景示例

假设移动设备实际位于货架通道A，但由于通道B结构相似，本次匹配同时产生A、B两个候选。

- 候选A与上一时刻主定位假设连续，且连续多次获得支持；
- 候选B虽然某一时刻的瞬时匹配代价略低，但缺乏连续支持；
- 系统保留两个假设，不因候选B单次更优就立即跳转；
- 后续移动过程中，候选A持续与设备运动一致，候选B逐渐失去支持；
- 系统确认候选A具有稳定性和唯一性后，才对地图—里程计对齐关系进行有界渐进校正。

如果A、B两个候选始终难以区分，则系统保持原校正并进入弱可信或Recovery状态，而不是任意选定一个货架通道。
可以。2.7的核心不是另外引入一套定位技术，而是规定：2.5和2.6得到的定位结果在什么情况下可以采用，定位不可靠时怎样重新定位，以及当前定位状态是否允许输出价签地图坐标。

## 2.7 定位状态转换与定位恢复

“状态机”和“Recovery”都属于工程术语。为了便于代理人理解，正文可以使用“定位状态转换规则”和“定位恢复”。如果需要保留英文，只在第一次出现时写成：

> 定位恢复（Recovery）是指移动终端在当前地图定位不可信时，通过扩大搜索范围、连续验证候选结果，重新确定自身在门店先验地图中位置的处理过程。

后文统一使用“定位恢复”，不要反复混用Recovery、Recovery Episode、恢复状态、恢复阶段等不同说法。

## 一、执行主体以及与价签自动定位的关系

本部分的执行主体是移动终端，例如安装MarketScanner的手机。更具体地说，是运行在移动终端中的“先验地图定位模块”。

正文首次出现时建议写成：

> 移动终端中的先验地图定位模块，以下简称“定位模块”。

后文统一使用“定位模块”，不再使用含义不清的“定位器”“系统定位器”或者“业务定位器”。

定位模块使用以下数据判断当前定位是否可靠：

- 连续里程计输出的设备位姿；
- 当前采集的局部结构点；
- 门店先验地图；
- 2.5产生的候选对齐关系；
- 2.6形成的定位假设；
- 移动终端当前的传感器跟踪状态。

本部分与价签自动定位有直接关系，但它不是独立于价签自动定位的第三个目的。

价签自动定位必须知道：识别到价签时，移动终端在门店先验地图中的位置是否可信。如果定位模块处于稳定状态，并且价签识别、深度测量和节点绑定等条件也合格，才能输出价签地图坐标。

如果定位模块处于弱可信、定位丢失或者恢复中：

- 移动终端仍然可以继续识别和保存价签条码内容；
- 仍然可以保存价签图像、深度及其对应的扫描节点；
- 但不能把当时计算出的地图坐标直接作为可信价签坐标发布；
- 后续只有在定位恢复或扫描后处理能够重新证明该坐标时，才能形成正式结果。

因此，2.7的作用是为价签自动定位增加一道定位可靠性判断，防止价签被错误地定位到相邻的相似货架上。

## 二、“定位状态”是谁的状态

本部分的定位状态，属于移动终端中定位模块的状态。

它表示的是：

> 当前门店先验地图与连续里程计之间的对齐关系，是否足以支持自动校正和价签地图坐标输出。

它不是以下对象的状态：

- 不是移动终端硬件自身的运行状态；
- 不是ARKit自身的跟踪状态；
- 不是某个SLAM节点的状态；
- 不是货架的状态；
- 也不是价签本身的状态。

ARKit等传感器的跟踪状态只是定位模块进行判断的一项输入。定位模块综合候选匹配、连续支持、传感器跟踪以及校正幅度等证据，得到最终的定位状态。

## 三、定位状态的统一中文含义

为避免前后用词不一致，建议全文统一使用以下名称。

|英文名称|统一中文名称|通俗含义|
|---|---|---|
|uninitialized|未初始化|尚未建立门店先验地图与连续里程计之间的对齐关系|
|initializing|初始化中|已有初步位置，但还没有得到连续观测的充分支持|
|usable|可用|已经得到可使用的地图定位结果，但稳定证据尚未完全积累|
|stable|稳定|连续多次观测均支持同一定位结果，可以进入正常工作|
|weak|弱可信|仍有大致位置，但当前证据不足以继续安全校正或输出价签地图坐标|
|lost|定位丢失|已经无法证明移动终端当前位于门店地图中的哪个位置|
|recovering|恢复中|定位模块正在扩大搜索范围，重新寻找可信位置|
|manualCorrection|人工校正中|由工作人员选择已知地图位置或方向，帮助重新建立对齐关系|

其中，“人工校正中”是可选实施方式。它不是本发明自动定位恢复方法的必要步骤。如果保留，建议放在2.7末尾单独说明，不要与主要自动流程混写。

还应当区分以下两个概念：

- “恢复中”是定位模块当前所处的定位状态；
- “一次定位恢复回合”是定位模块在恢复中执行的一次有开始条件、有结束条件的处理过程。

## 四、定位状态如何转换

定位状态的基本转换过程如下。

### 1. 从未初始化进入初始化中

移动终端加载门店先验地图，并获得初始位置、人工选择位置或者第一次候选匹配结果后，定位模块开始建立门店先验地图与连续里程计之间的对齐关系。

此时只有初步结果，尚未获得连续观测支持，因此进入“初始化中”。

### 2. 从初始化中进入可用

定位模块连续采集局部结构点，并按照2.5和2.6的方法进行地图匹配和定位假设判断。

当某一定位假设通过基本的结构、匹配质量和安全性判断后，定位模块得到一个可以使用的地图对齐结果，状态进入“可用”。

“可用”表示已有可信依据，但不等于已经允许自动发布价签地图坐标。

### 3. 从可用进入稳定

移动终端继续正常扫描。如果连续多次局部正常匹配均支持同一个定位假设，而且没有出现明显冲突，定位状态由“可用”进入“稳定”。

只有进入“稳定”后，才允许满足自身质量条件的价签观测进入自动定位结果。即使定位状态稳定，价签本身仍需通过识别质量、深度质量和节点绑定等条件。

### 4. 从稳定或可用进入弱可信

出现以下情况时，定位模块可以进入“弱可信”：

- 连续若干次实际匹配未找到可信的定位假设；
- 最佳候选与次佳候选过于接近，无法区分相似货架；
- 当前结构点数量或方向覆盖不足；
- 候选要求的校正幅度明显不符合连续运动；
- 移动终端的传感器跟踪质量下降；
- 当前观测与门店先验地图持续不一致。

弱可信表示系统仍保留之前的位置和原始轨迹，但暂时停止自动采用新的地图校正结果，并停止发布新的价签地图坐标。

### 5. 从弱可信进入定位丢失或恢复中

如果后续正常匹配重新获得充分证据，定位模块可以重新回到可用状态。

如果弱可信持续存在，或者已经无法维持可信地图位置，则：

- 进入“定位丢失”；或者
- 启动一次定位恢复回合，并进入“恢复中”。

### 6. 定位恢复成功后的状态

定位恢复成功后，定位模块先回到“可用”，而不是直接回到“稳定”。

这是因为扩大范围搜索虽然重新找到了可能正确的位置，但还需要由后续局部正常匹配再次验证。只有连续获得新的局部正常匹配证据后，才能重新进入“稳定”。

这种设置可以防止扩大搜索刚找到一个候选位置，就立即把它当作完全可靠的最终位置。

## 五、什么情况下启动定位恢复

不能把“弱可信或者定位丢失时可以恢复”写成没有条件的自动操作。建议明确为满足以下一种或者多种情况时，定位模块才启动定位恢复：

1. 连续多次实际执行的匹配均没有得到可信定位假设；
2. 定位状态已经由稳定或可用下降为弱可信，并且在一定时间或一定次数内未自行恢复；
3. 定位状态已经变为定位丢失；
4. 移动终端的传感器跟踪已经恢复，但门店先验地图与连续里程计之间的对齐关系仍然不可信；
5. 连续出现一个与当前位置相距较远、但跨帧重复出现的候选结果，普通局部搜索无法安全判断是否应当采用；
6. 工作人员主动请求重新定位。

这些触发条件的核心是：

> 普通局部匹配已经不能可靠确定位置，继续沿用旧结果或者直接采用某个远处候选都存在风险，因此需要启动范围更大的重新搜索。

进入定位恢复不等于采用某个远处候选。它只表示定位模块开始扩大搜索范围并重新积累证据。

具体连续次数、持续时间和触发间隔可以作为实施例参数，不宜写成本发明不可改变的必要条件。

## 六、一次定位恢复回合怎样执行

一次定位恢复回合可以按照以下步骤展开。

### 步骤1：建立定位恢复回合

输入包括：

- 当前定位状态；
- 连续未得到可信定位结果的次数；
- 当前传感器跟踪状态；
- 上一次定位恢复的结束情况；
- 是否收到人工重新定位请求。

定位模块判断是否满足恢复触发条件。满足条件时，建立一次新的定位恢复回合，并记录：

- 本次恢复的身份编号；
- 触发原因；
- 开始时间；
- 截止时间；
- 允许执行的有效搜索次数。

输出是一个已经建立、等待搜索的定位恢复回合。

这里的“截止时间”是指本次定位恢复最晚应当结束的时间。优选采用只向前累计、不受手机系统时间调整影响的计时方式。

“冷却期”是指一次自动恢复结束后，暂时不立即再次启动自动恢复，防止定位模块反复启动和退出恢复流程。

### 步骤2：检查本次观测是否可以用于恢复搜索

定位模块读取：

- 当前局部结构点；
- 当前连续里程计位姿；
- 门店先验地图；
- 当前传感器跟踪状态。

定位模块首先检查数据是否有效。例如：

- 是否有有效深度；
- 传感器跟踪是否可以使用；
- 结构点是否足够；
- 当前是否具备执行搜索所需的计算资源。

只有实际完成一次候选搜索，才计为一次有效搜索。

如果本轮没有有效深度、传感器跟踪不可用、结构点不足、计算任务繁忙或者本轮受到计算频率限制，则本轮只是等待，不应计为一次失败的恢复搜索，也不应消耗有效搜索次数。

### 步骤3：扩大搜索范围并产生多个候选

普通定位主要在当前预测位置附近进行局部搜索。进入恢复状态后，定位模块在更大的平移和偏航角范围内搜索。

处理过程仍然沿用2.5的方法：

1. 使用当前局部结构点查询门店先验地图距离场；
2. 从较粗的搜索范围开始查找可能位置；
3. 对较好的区域进一步细化；
4. 保留多个彼此分离的候选对齐关系；
5. 为每个候选计算匹配代价和质量信息。

输出是多个“候选对齐关系”，而不是一个可以直接采用的最终位置。

### 步骤4：跨帧建立并更新定位假设

定位模块将当前产生的候选对齐关系，与前几次恢复搜索形成的定位假设进行关联。

如果连续多次搜索均在相近位置和朝向得到候选，则相应定位假设的支持程度增加。

如果某一定位假设在后续搜索中不再得到支持，则增加其连续未关联次数。

该步骤的输出包括：

- 当前获得最多连续支持的定位假设；
- 排名第二的定位假设；
- 两者之间的质量差异；
- 各定位假设的连续支持情况。

定位模块不得因为某一次搜索中某个候选的代价最低，就直接把移动终端跳转到该候选位置。

### 步骤5：判断定位恢复是否收敛

这里的“收敛”不是指移动终端收敛，也不是指货架收敛。

通俗地说，定位恢复收敛是指：

> 连续几次有效搜索逐渐指向同一个定位答案，并且其他竞争答案已经明显不如该答案可信。

具体而言，定位模块检查：

- 是否连续多次支持同一个定位假设；
- 当前结构点是否充分；
- 匹配代价是否合格；
- 最佳定位假设是否明显优于次佳定位假设；
- 候选对齐关系是否在连续搜索中逐渐稳定；
- 候选要求的校正幅度是否符合设备可能的实际运动；
- 当前传感器跟踪状态是否可以支持校正。

本节所称的“收敛”，是“在线定位假设和地图—连续里程计对齐关系的收敛”。

它不是扫描结束后整个SLAM图优化的收敛。后者属于后续扫描后处理步骤，应当另行描述。

### 步骤6：记录证据并采用恢复结果

如果某一定位假设通过全部判断条件，定位模块先保存本次恢复过程及其判断证据，再将该定位假设标记为“已接受定位结果”。

随后，定位模块按照2.6的有界渐进校正方法，小步更新地图—连续里程计对齐关系，而不是一次性跳到候选位置。

更新完成后，定位状态先回到“可用”。后续获得连续的局部正常匹配证据后，再进入“稳定”。

### 步骤7：处理未恢复成功的情况

如果达到截止时间或者有效搜索次数上限后，仍然存在以下情况：

- 最佳和次佳定位假设无法区分；
- 没有定位假设获得连续支持；
- 当前结构不足；
- 候选要求的校正幅度不安全；

则本次定位恢复结束，但不采用任何新位置。

定位模块继续保持弱可信或者定位丢失状态，并：

- 保留原始连续里程计轨迹；
- 保留已识别的价签内容和原始测量；
- 保留恢复过程中产生的候选和拒绝原因；
- 停止自动更新地图—连续里程计对齐关系；
- 停止发布无法证明的价签地图坐标；
- 提示补充扫描或者进行人工校正。

## 七、“中间候选”与“扫描后绝对位姿约束”是什么

原文中的“中间候选”含义不够明确，建议统一改为：

> 待确认候选对齐关系。

它是定位恢复过程中搜索得到、但尚未通过连续支持、唯一性和安全性判断的候选结果。

待确认候选只能用于：

- 后续定位假设跟踪；
- 与其他候选进行比较；
- 记录定位恢复的处理过程；
- 分析定位恢复失败原因。

待确认候选不能直接修改正式地图定位，也不能直接用于计算正式价签地图坐标。

原文中的“正式后处理绝对约束”建议统一改为：

> 扫描后绝对位姿约束。

扫描后绝对位姿约束是指：定位结果已经通过可信判断，并且已经绑定到某一个确定的同步定位与建图（SLAM）节点后，形成的“该节点在门店先验地图中应位于何处”的约束信息。

它至少说明：

- 对应哪个SLAM节点；
- 该节点在门店先验地图中的位置和朝向；
- 该定位结果已经被接受；
- 该定位结果的质量或不确定程度；
- 该结果来自哪一次定位观测或定位恢复。

只有“已接受定位结果”才能进一步形成扫描后绝对位姿约束。待确认候选和未成功的定位恢复结果只能保存为过程证据，不能进入正式图优化。

## 八、图6英文词语的统一中文含义

图6建议直接改成中文。必须保留英文时，可采用以下对应关系：

- `uninitialized`：未初始化；
- `initializing`：初始化中；
- `usable`：可用；
- `stable`：稳定；
- `weak`：弱可信；
- `lost`：定位丢失；
- `recovering`：恢复中；
- `Recovery Episode`：一次定位恢复回合；
- `deadline`：截止时间；
- `cooldown`：冷却期；
- `provisional`：待确认；
- `accepted`：已接受；
- `provisional ≠ accepted`：待确认不等于已接受；
- `Local`：局部正常匹配；
- `manualCorrection`：人工校正。

图6标题建议改成：

> 图6 定位状态转换与定位恢复流程

此前回答文档中还列出了“恢复触发”“扩大范围搜索”“候选区域”“定位假设跟踪”等概念。这些概念可以用于解释处理步骤，但它们并不是当前图6中实际显示的全部英文框名，不建议突然塞入图6术语表，以免代理人误以为又出现了新的模块或数据对象。

## 九、图6的数据流向

图6的数据流可以统一解释为：

```
加载门店先验地图和初始位置
        ↓
未初始化
        ↓
初始化中
        ↓
获得一个通过基本判断的地图定位结果
        ↓
可用
        ↓
连续局部正常匹配支持同一定位结果
        ↓
稳定
        ↓
结构匹配、唯一性或跟踪质量下降
        ↓
弱可信
   ↙            ↘
恢复搜索成功      持续无法定位
   ↓              ↓
可用          定位丢失
   ↑              ↓
   └────定位恢复───┘
```

定位恢复成功后必须先回到可用，再经过新的局部正常匹配进入稳定。定位恢复没有成功时，保持弱可信或定位丢失，不能输出无法证明的价签地图坐标。

## 十、结合平行货架场景说明

例如，移动终端原来稳定定位在第三条货架通道。由于顾客遮挡或者设备快速转弯，当前结构点与门店先验地图的匹配质量下降，定位状态进入弱可信。

此时移动终端仍然继续保存连续里程计轨迹和识别到的价签，但不再直接发布新价签的地图坐标。

如果普通局部匹配不能恢复，定位模块启动一次定位恢复回合。扩大搜索后，系统在第三条和第四条货架通道分别找到一个候选对齐关系。

后续连续搜索中：

- 第三条通道对应的候选持续出现，位置和朝向逐渐稳定；
- 第四条通道对应的候选只偶尔出现，或者匹配质量不稳定。

定位模块据此判断第三条通道对应的定位假设收敛，将其标记为已接受定位结果，并小步更新地图—连续里程计对齐关系。

更新后，定位状态先回到可用。经过后续连续局部正常匹配验证后，再进入稳定，恢复价签自动定位。

如果两个通道的候选始终难以区分，定位模块不选择其中任何一个，而是保持弱可信或定位丢失，继续保留原始数据并要求补充扫描或人工校正。


# 2.8


## 2.8.1 条码识别得到哪些信息

payload在这里统一称为“条码内容”，就是解码后读出的文字或编号，例如“TAG0007”。它不表示价签坐标，也不一定直接包含商品价格。frame ID统一称为“帧编号”，用来区分不同采集帧。

| 原词或原表述 | 统一用词及含义 |
| --- | --- |
| payload／payloadStringValue | 条码内容：识别程序从二维码或条形码中读出的字符串。 |
| symbology／码制 | 条码制式：编码的种类，例如二维码QR、EAN-13条形码。 |
| bounding box／normalized bounds | 条码区域：条码在图像中的矩形范围。归一化表示用0到1的比例记录位置和宽高。 |
| frame ID／frameID | 帧编号：标识某一个采集帧。连续拍到同一价签，也必须使用不同帧编号。 |
| observation ID | 观测编号：标识某一条价签观测。一帧识别出两个价签，可有相同帧编号和不同观测编号。 |

### 移动终端如何读出条码

移动终端从同一相机采集流取得图像，调用Apple Vision（苹果图像识别框架）的条码识别功能，在指定图像区域查找并解码条码；没有可靠结果时，可扩大邻近识别区域再尝试。输出为条码内容、条码制式和条码区域，并关联该采集帧的帧编号。

采集帧对应ARFrame，即苹果增强现实框架ARKit提供的一组同时刻数据，包括图像、可用深度、相机参数、相机位置和朝向等。后续测量使用与识别图像对应的数据，避免把前一帧的条码区域配上后一帧的深度或相机位置。

## 2.8.2 怎样过滤深度并找出同一表面


深度可以理解为相机看到的某个图像位置离相机有多深。条码区域中可能同时包含价签表面、后方货架和前方遮挡物，因此移动终端需要读取多个深度值，先排除明显无效的值，再判断其余值是否来自同一表面。

### 步骤1 在条码区域内取样

输入是上一节得到的条码区域，以及该采集帧对应的深度图。移动终端先把条码区域四边向内收缩，减少边缘背景混入，再在区域内均匀取点。参考实施例按宽高各向内收缩18%，布置9×9共81个采样点。图像和深度图尺寸不同时，应把条码区域换算到深度图的对应位置。

### 步骤2 删除不能使用的深度值

移动终端排除无法表示有效数值的深度，以及小于0.2米或大于8米的深度。若有与深度图正确对应的置信度图，还要排除最低置信度的采样点。置信度图表示深度传感器对各点测量的可靠程度。

没有置信度图时，只跳过这一项筛选，仍要执行其余深度检查；若置信度图存在但尺寸或格式不匹配，则该帧深度测量不能使用。此步骤输出“有效深度样本”，即通过初步筛选的深度值，尚不能直接认定为价签表面。

这里的“识别”是指移动终端怎样识别出属于同一表面的深度值。原文“一致表面簇”统一解释为“同一表面的深度组”，即一组深度相近、可能来自价签所在表面的采样值。

### 步骤3 将深度相近的样本归为一组

移动终端把有效深度样本从近到远排序，查找深度跨度不超过0.10米、且至少包含12个样本的深度组。存在多个满足条件的组时，先选较近的一组；两组深度相差不超过0.02米时，优先选择样本较多的一组。

### 步骤4 检查所选组是否与整体深度一致

选好后，移动终端再检查事先计算出的全部有效深度样本中位数，看它是否位于所选组的深度范围内。若不在，说明较近的一组可能只是遮挡物，或者图像中存在无法明确区分的前后表面；此时不把该组直接当作价签深度。

例如，有效深度主要分为约1米和约2米两组。如果选中约1米的近处组，但全部有效样本的中位数却约为2米，移动终端就拒绝这次深度选择。通过检查后，再把所选组交给下一节的统计判断。

## 三 怎样判断选出的深度是否可信

以下统一使用“中位数”，不再与“中值”交替。移动终端既检查样本够不够多，也检查它们是否集中在同一个深度附近。样本数量多，并不自动表示测量可信。

| 指标 | 计算方式及含义 |
| --- | --- |
| 中位数 | 把深度从小到大排列，取中间值；偶数个样本取中间两个值的平均。用它表示该组的典型深度，可减小少量异常值的影响。 |
| MAD<br>中位绝对偏差 | 先计算每个样本与该组中位数相差多少，再对这些差值取中位数。MAD越小，表示该组深度越集中。英文全称为Median Absolute Deviation。 |
| 内点 | 与该组中位数的差值没有超过允许范围的样本，即仍可用于这次深度判断的样本。 |
| 内点比例 | 内点数除以初步筛选后的全部有效深度样本数。分母不是81个原始取样点，也不是只计算所选深度组的样本数。 |

### 移动终端按什么顺序计算

第一步，接收上一节选出的深度组，计算该组中位数和MAD。
第二步，以“0.02米和3倍MAD中的较大值”作为允许偏差，保留处于该范围内的样本，并统计内点数和内点比例。
第三步，同时检查：内点至少12个、内点比例不低于55%、MAD不大于0.045米。通过后，才继续进行三维位置计算和表面检查。

### 用一组数字说明

为便于理解，假设81个采样点经过初步筛选后剩下60个有效深度样本，其中40个集中在1.18到1.22米，另外20个在更远的背景。所选近处组的中位数是1.20米，MAD是0.01米，且已经通过上一节的整体中位数检查。

允许偏差取0.02米与3×0.01米中的较大值，即0.03米。因此，处在1.17到1.23米范围内的这40个样本被保留，内点比例为40÷60，约66.7%。内点数、内点比例和MAD均满足上述示例条件，深度统计检查通过。

如果仍是60个有效样本，但只有25个内点，内点比例就只有41.7%。即使这25个样本彼此接近，也不足以通过55%的条件，移动终端不能据此声称已经测准价签位置。

### 这一检查的输出是什么

输出包括典型深度、有效样本数、内点数、内点比例、MAD，以及通过或未通过的结果。它们随后用于三维位置计算、表面检查和测量置信度记录。深度统计检查通过，还不等于最终价签坐标已经可以使用。

补充实施细节：参考实现为后续三维点和平面计算重新筛点时，允许偏差采用“0.025米和3倍MAD中的较大值”。它与前面统计检查的0.02米用途不同，不能把两者写成同一个阈值。

## 四 怎样计算三维位置并检查表面



移动终端先把图像中的采样位置和可信深度换算为相机坐标中的三维点，再检查这些点是否大致落在同一小块平面上。这里的平面是价签区域附近的局部表面，不是重新生成整张货架地图。

### 步骤1 将像素位置和深度换算为三维点

输入为像素位置(u，v)、该位置的可信深度z，以及同一采集帧的相机内参K。相机内参可以理解为相机的成像标定参数，用来把图像位置换算为空间中的观察方向。原交底书式（5）为：

![[Pasted image 20260914115306.png]]

式中，p表示价签三维点，上标camera表示“相机坐标”，下标tag表示“价签”；K⁻¹表示相机内参矩阵的逆；\[u v 1]ᵀ表示把像素位置写成一个列向量。这个式子的意思是：先确定该像素朝向哪里，再用深度确定沿这个方向的空间位置。z按该相机模型的深度定义使用，不能随意替换为另一种距离。

### 步骤2 用三维点构造近似局部平面

移动终端使用筛选后的三维点，按采样位置选取横向最左和最右、纵向最上和最下的代表点。左右两点形成横向向量，上下两点形成纵向向量；对两个向量做叉乘，可得到一个同时垂直于它们的方向，再将其长度归一为1。这个垂直方向就叫“表面法向”。

可以把表面法向理解为垂直插在价签表面上的一根短箭头，它描述表面的朝向。移动终端再计算这些三维点的平均位置，称为质心；让平面经过这个质心，并以上述表面法向作为垂直方向，就确定了近似局部平面。

### 步骤3 检查这个平面是否可信

移动终端先检查表面法向能否正常计算，包括数值是否有效、长度是否接近零。再计算各三维点到该平面的距离，对这些距离取中位数，作为“平面残差”。平面残差越小，表示多数点越贴近该平面。参考实施例要求平面残差中位数不超过约0.06米。

例如，平面残差中位数为0.01米，说明一半以上的采样点距该平面不超过约1厘米；若为0.10米，就超出了上述条件，不能把这次表面检查当作通过。

### 检查失败后怎样处理

失败时记录“深度表面不稳定”，不将这次深度测量标为可靠位置。若具备已知货架平面且满足相交条件，可改用相机观察方向与该平面的交点计算位置；否则标为“无法形成可用位置”。条码内容及失败原因仍可保留。

价签三维点选用保留样本中深度最接近该组中位数的点；平面质心仅用于平面检查，二者不要混用。上述方法属于利用代表点构造近似局部平面。不要把它改写成已经使用复杂的最小二乘平面拟合或随机抽样一致性算法，也不要把“保留原始深度记录”写成“深度定位已经成功”。

## 五 怎样将一次观测与节点对应保存

“绑定”就是把某次价签观测与它实际对应的扫描节点、采集时间和扫描身份一起保存，使后续能够找到正确的原始测量。为理解这一过程，需要先区分采集帧、扫描节点和价签观测。

| 对象 | 含义 |
| --- | --- |
| 采集帧 | 相机在某一时刻取得的一组数据，包括图像及其对应的深度、相机位置和朝向等。 |
| 扫描节点<br>SLAM node | 扫描过程中，SLAM（同步定位与建图）保存的一个带编号的位置和朝向记录。可把它理解为扫描轨迹中的一个记录点；它不是货架上的点。 |
| 价签观测 | 移动终端在一个采集帧中，对一个价签形成的一次识别、测量及关联记录。 |

### 步骤1 保存同一采集帧的数据

移动终端取得条码内容、条码区域、深度测量、相机位置和朝向、采集时间，以及当时的先验地图定位状态。识别计算可以稍后完成，但使用的数据必须对应原来的采集帧，不能在计算完成时临时换成最新帧的数据。

### 步骤2 取得一个明确的扫描节点

移动终端从同一次节点读取结果中，取得节点编号、节点时间、节点所属的扫描地图编号，以及节点的位置和朝向。一次完整读取是为了避免节点编号已经更新、位置和朝向却仍来自旧节点。采集帧和扫描节点不必一一对应，二者的时间也不必完全相等，但必须保存准确对应关系并满足时间检查。

### 步骤3 将价签位置写成相对该节点的位置

移动终端利用该采集帧的相机位置和朝向，把价签相对相机的位置换算到扫描坐标中，再利用已选节点的位置和朝向，换算成“价签相对绑定节点的位置”。这里包含坐标方向转换和平移，不能在有旋转时只做坐标相减。

直观地说，就是记住“价签位于这个扫描节点的什么方向、离它多远”。同时写下绑定节点编号。后续即使扫描轨迹被校正，仍能用该编号找到同一个节点。

### 步骤4 固定保存原始观测

移动终端把观测编号、帧编号、绑定节点编号、时间、先验地图版本、楼层、跟踪会话、测量结果和质量记录一起保存。这就是原文“冻结”的意思：保存当时的事实，后续校正不悄悄改写原记录；它不表示停止相机采集。

### 为什么要这样保存

假设某观测绑定到节点208。扫描结束后，轨迹优化把节点208的位置和朝向修正了，移动终端就用修正后的节点208和原来保存的相对位置，重新计算价签地图位置。它按绑定节点编号查找，不再猜测哪个节点时间最近。这样既能更新最终位置，又能保留原始测量依据。

## 六 原始观测中的字段分别是什么意思


这些字段分别回答：是哪一次测量、来自哪一帧、对应哪个节点、用了哪张先验地图，以及当时的测量是否可靠。本页解释身份和原始测量字段，下一页解释定位状态、测量方法和深度质量。

| 原词或字段 | 统一中文 | 具体含义 |
| --- | --- | --- |
| observation ID | 观测编号 | 区分不同的价签观测；不是商品编号。 |
| frame ID | 帧编号 | 说明该观测来自哪一个采集帧；用于避免把同一帧重复当作多帧。 |
| SLAM boundNodeID | 绑定节点编号 | 说明该观测对应哪个扫描节点，后续据此查找最终节点位置和朝向。 |
| node stamp | 节点时间 | 扫描节点自身记录的时间。另存“帧时间”表示采集帧的时间；两种时间须正确对应。 |
| 地图ID | 先验地图编号 | 标识本次使用的门店先验地图。应与节点所属的扫描地图编号区分。 |
| 地图SHA | 先验地图摘要 | SHA是安全散列算法的英文缩写。这里指计算地图文件内容得到的摘要值，用于核对具体地图版本。 |
| 楼层 | 楼层编号 | 说明这次观测属于门店的哪一层，防止跨楼层混用。 |
| tracking session | 跟踪会话 | 标识一次连续的设备跟踪过程，避免把不同跟踪过程中的帧、节点和坐标混在一起。 |
| 原始相机测量 | 价签相机坐标 | 在这次采集时，价签相对于相机的三维位置。 |
| 原始地图测量 | 采集时的价签地图坐标 | 按采集当时的定位结果换算得到的地图位置；属于当时结果，不当然等于最终位置。 |
| 节点局部坐标 | 价签相对绑定节点的位置 | 保存价签与绑定节点的相对关系，供后续根据修正后的节点位置和朝向重新计算。 |

ID统一解释为“编号”，用于识别对象；SHA统一解释为“摘要”，用于核对内容版本。编号相同不能单独证明地图内容相同，因此需要同时保存和检查先验地图编号及摘要。

“先验地图”是扫描前已有的门店地图；“扫描地图”是本次扫描过程中形成的地图。全文提到地图编号时，应明确是哪一种，避免把两个编号混用。

## 七 定位状态 测量方法和深度质量

继续回答批注112。三者分别说明移动终端在地图上定位得怎样、价签三维位置怎样算出、深度测量本身是否可靠，不能互相替代。

### 定位状态和定位置信度

“定位状态”统一指先验地图定位状态，即移动终端认为自己与先验地图的对应关系处于什么阶段。“定位置信度”表示对该定位结果可靠程度的评价；它不是条码识别置信度，也不是深度测量置信度。

| 英文状态 | 中文含义 |
| --- | --- |
| uninitialized／initializing | 尚未初始化／正在初始化，尚未建立或正在建立地图对应关系。 |
| usable／stable | 可用／稳定，表示已有可用的地图定位，或连续定位证据已较稳定。 |
| weak／lost | 定位变弱／定位丢失，当前地图位置不够可靠或已无法维持可信定位。 |
| recovering／manualCorrection | 正在恢复定位／人工校正阶段，分别表示重新寻找可靠定位或处理人工校正。 |

还要另存“设备跟踪状态”（ARKit trackingState），表示相机和惯性传感器的跟踪是否正常。normal表示正常，limited表示受限，notAvailable表示不可用。设备跟踪正常，也可能尚未在先验地图上找准位置，因此它与先验地图定位状态分开记录。

### 测量方法

“测量方法”统一指价签三维位置的计算方法。保存时应使用实际采用的方法，不把程序内部函数名当作测量方法取值。

| 保存的英文取值 | 中文含义 |
| --- | --- |
| smoothed_scene_depth | 平滑场景深度：利用经过平滑处理的场景深度计算价签三维位置。 |
| scene_depth | 原始场景深度：利用未经过上述平滑处理的场景深度计算位置。 |
| shelf_plane_ray | 货架平面求交：已知货架平面时，计算相机穿过条码位置的观察方向与该平面的交点。 |
| unavailable | 无法形成可用位置：当前没有满足要求的三维测量结果。 |

### 深度质量和测量置信度

“深度质量”包括有效样本数、内点数、内点比例、MAD、平面残差和表面法向。英文plane_residual_m表示以米记录的平面残差；surface_normal_camera表示在相机坐标中记录的表面法向。各项含义及计算过程见前文。

“测量置信度”（measurement confidence）是对这次三维测量可靠程度的评价。深度表面检查失败时，记录unstable_depth_plane，中文为“深度表面不稳定”，并按实际结果改用货架平面求交或标为无法形成可用位置。原回答中的withPlane只是内部方法名，不作为独立的保存取值。

## 八 图7中的多帧指什么

是同一次扫描中，移动终端相机连续取得的多个采集帧。这些帧与扫描过程使用的相机数据来自同一采集流。每一帧分别识别条码、测量深度并记录节点对应关系；不是扫描结束后从点云中任意抽取几组点，作为新的“帧”。

Tag Burst统一称为“价签多帧观测组”，表示短时间内围绕同一价签收集的多次独立观测。Burst在这里不另译为“爆发”或“突发”。每个有效采集帧有不同的帧编号，重复处理同一张图像不能增加有效帧数。

### 采集帧与扫描节点有什么关系

相机采集一帧，不代表一定新建一个扫描节点。多个采集帧可以绑定同一个节点，也可以绑定不同节点；节点编号连续不是组成观测组的必需条件。

| 采集帧 | 观测编号 | 绑定节点 | 条码内容 |
| --- | --- | --- | --- |
| 第1001帧 | 观测A | 节点208 | TAG0007 |
| 第1002帧 | 观测B | 节点208 | TAG0007 |
| 第1003帧 | 观测C | 节点209 | TAG0007 |

这是便于理解的示例：三帧分别测量同一价签，前两帧对应同一节点，第三帧对应另一节点。它们可以组成一个价签多帧观测组，但还要确认条码制式、先验地图版本、楼层和跟踪会话一致，且时间连续。参考实施例要求相邻观测时间间隔在0到3秒；时间倒退或超过3秒时结束当前组，重新建组。

条码内容相同只是核对身份的一部分，不能单凭相同商品条码，就把门店不同位置的两个实体价签当成同一个。上例以连续观察同一实体价签为前提。组成观测组后，还需经过第2.9节规定的完整性和质量检查，不能只数够三帧就认定最终坐标可靠。

### 按图7顺序理解整个过程

移动终端先从采集帧读出条码内容和条码区域；再在对应深度图中取样，筛选同一表面的深度并检查质量；随后计算价签相机坐标、检查局部表面，并换算为价签相对绑定节点的位置。移动终端逐帧保存这些原始观测，再把短时间内符合条件的观测组成价签多帧观测组。

扫描结束后，移动终端根据绑定节点编号取得修正后的节点位置和朝向，重新计算价签地图位置，再检查结果是否可用。因此，在线显示的位置可以更新，原始观测保持不变；深度或定位证据不足时，保留条码和原因，不能把不可靠位置直接当作最终结果。

