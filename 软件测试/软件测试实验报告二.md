任务一
在
```java
// 设置mock对象的行为
when(mockStrategy.move(any(Board.class))).thenReturn(Board.UP);
// 执行测试
int move = mockStrategy.move(board);
// 验证结果
assertEquals(Board.UP, move);
// 验证方法被调用
verify(mockStrategy, times(1)).move(any(Board.class));
```
中`Strategy`为
```java
public interface Strategy {
    Board play(Board board);
}
```

错误：
1,调用了不存在的方法
Strategy中没有`move(Board)`
2,返回值类型错误 play() 返回为 Board 而非int

在
```java
@Test
@DisplayName("测试RandomStrategy的性能")
@Timeout(5) // 超时5秒
public void testStrategyPerformance() {
	Strategy strategy = new RandomStrategy();
	Board testBoard = new Board();
	// 执行多次移动
	for (int i = 0; i < 10; i++) {
		int move = strategy.move(testBoard);
		testBoard = testBoard.move(move);
		if (testBoard.isTerminal()) {
			break;
		}
	}
	assertTrue(true, "性能测试完成");
}
```
1,未使用 Mockito
2，`Strategy.move()` 不存在
3， 错误理解了 `Strategy` 的职责
4， `Board.isTerminal()` 应该是 `isStuck()`

prompt
```
你正在处理一个 Maven Java 课程项目 mcts-2048。请完成“任务一：Mockito 代码修改”，直接实施、测试并给出验证证据。
任务目标：
修正 ExampleTest.java 中以下两个带有 
TODO: "Something is wrong with Mockito." 的测试： 
1. testMockStrategy()
2. testStrategyPerformance() 
必须先分析原示例为什么错误，再实施修改。 

实现要求：
1. testMockStrategy 必须：
- 正确初始化并使用 mockStrategy； 
- 对 play(Board) 设置确定的返回行为； 
- 验证返回的 Board； 
- 验证调用参数和调用次数； 
- 必要时使用 same、eq 或 ArgumentCaptor； 
- 不得只断言对象非空； 
2.testStrategyPerformance 必须：
- 不依赖随机结果；
- 不编写可能无限运行或偶发超时的测试； 
- 使用 JUnit 5 的 assertTimeout 或等效超时机制； 
- 包含有意义的结果或交互断言；
  
执行流程：
1. 阅读上述源码和 pom.xml。
2. 说明两个 TODO 的具体错误原因。
3. 修改 ExampleTest.java。
4. 运行： mvn -Dtest=ExampleTest test
5. 再运行： mvn clean test
6. 如果失败，定位并修复测试侧问题；不要擅自修改生产代码。

验收条件：
两个 TODO 已被真实测试替换
使用正确的 Strategy.play(Board) API；
测试具有确定性
ExampleTest 定向测试通过
没有修改授权范围外的文件
```
![[Pasted image 20260917202442.png]]

任务二
prompt：
```
你正在处理 Maven Java 项目 mcts-2048。请完成“任务二：使用 LLM 生成并迭代 BitBoards 测试”，直接实施、运行 JaCoCo 和 PIT，并用本轮生成的证据判断是否达标。

一、目标指标

针对 BitBoards.java，而不是整个项目：

- 所有任务二测试执行通过；
- 行覆盖率 >= 90%；
- 分支覆盖率 >= 90%；
- Mutation Score >= 60%。

不得用整个项目的汇总指标代替 BitBoards 类级指标。

二、允许修改范围

默认只允许修改BitBoardTest.java,如果确有必要，可以在同一测试包中新建纯测试辅助类，但优先复用TestUtils.java

不得修改：

- src/main/java 下的生产代码；
- pom.xml；
- ExampleTest.java；
- BoardTest.java；
- MeasureTest.java；
- README、教学文档；
- 已有报告来伪造本轮结果。

如果正确测试暴露出生产代码缺陷，不要通过断言当前错误行为来掩盖；

三、必须先阅读

- pom.xml
- BitBoards.java
- Board.java
- BitBoardTest.java
- TestUtils.java
- README.md
- TESTING_GUIDE.md
- MAVEN_COMMANDS.md

四、测试设计要求

请先列出测试矩阵，再编写测试。至少分析这些公开行为：

1. trans(long)
   - 明确棋盘转置结果；
   - 转置两次恢复原棋盘；
   - 空棋盘、非对称棋盘和边界数值。

2. reverse(long)
   - 明确反转结果；
   - 反转两次恢复原棋盘；
   - 各行独立反转。

3. move(long, int)
   - UP、RIGHT、DOWN、LEFT；
   - 空棋盘；
   - 无法移动的棋盘；
   - 单次合并；
   - 连续相同数字不得在一次移动中重复合并；
   - 多行同时移动；
   - switch 默认分支的实际行为及合理性。

4. frees(long)
   - 空棋盘；
   - 满棋盘；
   - 单个非空格；
   - 高位和低位包含零的混合棋盘；
   - 不要仅测试当前实现恰好返回正确的布局。

5. canDirection(long, int)
   - 每个方向分别包含可移动和不可移动场景；
   - 断言具体方向，不要只循环后断言“至少一个为真”。

6. isStuck(long)
   - 真正无法移动的棋盘；
   - 仍有空位的棋盘；
   - 满棋盘但存在可合并方块；
   - 检查方法名、实现和 2048 规则是否一致。

7. spawn(long)
   - 仅在有空位时测试；
   - 验证恰好新增一个非零格；
   - 原有格子不变；
   - 新值只能为 1 或 2；
   - 不断言随机位置；
   - 不用概率阈值制造偶发失败。

8. pickRandomly()
   - 只验证稳定性质，例如返回值集合；
   - 不要求某个小样本中必须出现 2；
   - 不用睡眠或重复直到成功的循环。

9. print(long)
   - 必要时捕获 System.out；
   - 使用精确、跨平台可解释的输出断言；
   - 测试后恢复 System.out。

优先使用参数化测试、@DisplayName、AAA 结构和 TestUtils.parseb()。每个测试应验证一个清晰行为，避免把大量不相关断言塞进同一个方法。

五、证据要求

最终必须给出本轮数据：

- BitBoards 行覆盖率；
- BitBoards 分支覆盖率；
- BitBoards 变异体总数；
- KILLED 数；
- SURVIVED 数；
- NO_COVERAGE 数；
- Mutation Score；
- 报告文件的实际路径；
- 为杀死重要存活变异新增了哪些测试。
```

result：
![[截屏2026-09-18 10.49.24.png]]
![[截屏2026-09-18 10.51.34.png]]
任务三：
prompt
```
请完成“任务三：缺陷分析”，从单元测试角度找出至少一个真实生产缺陷，并用确定性测试稳定复现。

目标： 
1. 找出至少一个项目中的真实缺陷； 
2. 编写稳定、可重复的 JUnit 5 测试复现缺陷； 
3. 说明： 
- 缺陷所在类和方法； 
- 触发输入和前置条件； 
- 按 2048 规则或 API 语义得出的期望结果； 
- 当前实际结果； 
- 根因； 
- 测试执行结果。
  
缺陷选择标准： 
最终选择的主缺陷必须： 
- 能用很小的输入稳定复现； 
- 不依赖随机数恰好取某个值； 
- 不依赖机器速度； 
- 不需要网络或外部服务； 
- 不使用反射修改私有状态； 
- 具有明确的期望值和实际值； 
- 连续运行结果一致。 
优先选择纯函数或确定性状态转换缺陷。不要把代码风格、性能猜测或等价 PIT 变异当成真实缺陷。

```
result:
```markdown
**缺陷：`Board.canDirection(int)` 将所有非法走法报告为合法**
 
**位置**：`dk.ahle.thomas.mcts2048.Board#canDirection(int)`，`Board.java` 第 113–122 行，缺陷判定位于第 116–117 行。

**现象**：该方法用于回答"当前棋盘能否朝指定方向移动"。实测发现，只要棋盘不是完全满盘，它对 UP、RIGHT、DOWN、LEFT 四个方向**一律返回 `true`**，因而无法区分合法与非法走法。最小触发输入是默认构造的空棋盘：`new Board().canDirection(Board.UP)` 返回 `true`，而棋盘上没有任何方块，不存在可执行的走法。

**期望与实际**：以本代码库自身的定义为基准——`Board.move(m).changed` 由 `unsafe_move` 在确有方块移动或合并时置位——空棋盘的四个方向均应为 `false`，实际均为 `true`。在 8 个代表性布局 × 4 个方向共 32 次检查中，`canDirection` 与 `move().changed` 矛盾 **20 次**；同项目的位棋盘实现 `BitBoards.canDirection`（定义为 `move(b,m) != b`）在同一批检查中与真值矛盾 **0 次**，即两个实现给出相反答案。唯一被正确处理的是完全满盘。

**根因**：遍历 `orders[move]` 时只检查目标格 `grid[p+dir] == 0`，从未检查源格 `p` 自身是否持有方块。当 `p` 与 `p+dir` 同为空格时，第 116 行成立并立即返回 `true`，把"两个相邻空格"误判为"此处有方块可前移"。同一类中真正执行移动的 `unsafe_move` 恰好写有这条守卫（`Board.java` 第 62–63 行 `if (grid[src] == 0) continue;`），可见是漏写而非设计取舍。由此可推出精确失效条件：存在 `p ∈ orders[move]` 使 `grid[p] == 0` 且 `grid[p+dir] == 0`。该方法只会误报、不会漏报，故满盘时结果正确，与实测一致。

**复现与稳定性**：`src/test/java/dk/ahle/thomas/mcts2048/test/BUGReportTest.java`（5 个 `@Test` + 2 个 `@ParameterizedTest`，展开 69 个用例）从七个角度复现该缺陷。全部断言陈述正确行为，因此**预期失败**：`mvn -Dtest=BUGReportTest test` 连续两次执行均得到 `Tests run: 69, Failures: 45, Errors: 0, Skipped: 0`，剔除耗时字段后两次失败明细逐行相同。测试不含随机数、不含超时、不含 `@Disabled`、不含对错误结果的断言，`Errors=0` 说明失败全部来自断言而非环境异常。其中 `canDirectionBreaksAsSoonAsTwoAdjacentCellsAreEmpty` 用满盘死局 `1234/5678/1234/5678`（`canDirection(LEFT)` 正确返回 `false`）与仅清掉末行两个相邻方块的 `1234/5678/1234/56..`（LEFT 仍非法，却返回 `true`）对照，4 条断言中 3 条通过、仅缺陷那条失败，将根因隔离到"两个相邻空格"这一最小条件。

**建议修复**：在 `canDirection` 的循环体开头补上与 `unsafe_move` 一致的守卫 `if (grid()[p] == 0) continue;`。

**影响评估**：`Board.canDirection` 目前在 `src/main` 中无生产调用方——`RandomStrategy`、`GreedyStrategy`、`UCTStrategy` 使用的是实现正确的 `Board.isStuck()` 或 `pickMove() == null` 判据，故缺陷当前处于潜伏状态，未影响现有对局结果。但它是公开 API 且契约被违反，任何后续将其用于走法生成或搜索剪枝的改动都会立即受到影响：搜索会把所有非满盘的全部四个方向都视为可扩展分支。
```
